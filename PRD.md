# Osaka Travel Guide · PRD

**v0.4** (2026-08-09) · 일본 여행 매거진 시리즈 Vol. III

> **이 문서는 "제품이 무엇인가"의 정본.** 운영 [`CLAUDE.md`](CLAUDE.md) · 여행 사실·톤 [`CONCEPT.md`](CONCEPT.md) · 디자인 [`design.md`](design.md) · 회고 [`learning.md`](learning.md) · 이식 내역 [`OSAKA_TRANSPLANT_GUIDE.md`](OSAKA_TRANSPLANT_GUIDE.md) · 인덱스 [`DOC-MAP.md`](DOC-MAP.md).

---

## 0. Document Map

- §1 한 줄 정의 · §2 왜 만드는가 · §3 사용자
- §4 기능 요구사항 F1~F7
- §5 비기능 요구사항 · §6 데이터 요구사항
- **§7 범위 밖** — 요청이 와도 먼저 확인할 목록
- §8 성공 기준 · §9 버전 히스토리 · §10 참조

---

## 1. 한 줄 정의

**같이 가는 사람 전원이 링크 하나만 열면 되는, DAY 기반 읽기 전용 오사카 가이드.**

---

## 2. 왜 만드는가 — 문제 정의

동행과 여행할 때 반복되는 마찰:

| 문제 | 지금까지의 대처 | 왜 실패하나 |
|---|---|---|
| "오늘 어디 가?"를 매번 물음 | 카톡으로 링크 투척 | 스크롤 올려서 다시 찾아야 함. 링크가 대화에 묻힘 |
| 분 단위 동선표를 짜서 공유 | 노션·엑셀 일정표 | 한 곳만 어긋나도 전체가 무너짐. 지키느라 여행이 노동이 됨 |
| "여기 어디야?" | 이름 검색 | 일본어·영문명을 몰라 검색이 안 됨 |
| 동행이 스스로 못 고름 | 리더 한 명이 계속 결정 | 리더는 피곤하고 동행은 수동적 |

**설계 전제**: 동선을 짜주지 않는다. **구역 하나를 고르게 하고, 그 안에서 스스로 고르게 한다.**

### v0.6 — 전제의 한계가 드러난 지점

이 전제는 **날짜가 길고 목적이 열린 여행**을 상정한 것이었다. 실제 여행이 **2박 3일 · 실질 28.5시간 · 테마 3축 고정**으로 확정되자 전제가 안 맞는 구간이 생겼다:

- 온전한 하루가 1일뿐 → 구역 선택의 자유도가 사실상 없다
- 시설 마감(역사박물관 17:00)과 **배 시각(8/26 15:00 집결)** 은 어기면 여행이 깨진다
- 4명이 움직이면 "알아서 고르자"가 결정 지연으로 바뀐다

**그래서 v0.6에서 시간표(`zone.schedule`)를 넣었다.** 다만 원래 의도는 유지한다 —
**시각을 박는 것은 ① 시설 마감 ② 배·셔틀처럼 놓치면 끝나는 것 뿐**이고, 나머지는 순서와 대략적 시작점만 준다. 늦으면 뒤로 밀면 되는 구조를 지킨다.

> 분 단위로 전부 못 박는 순간 § 2의 "한 곳만 어긋나도 전체가 무너진다"로 되돌아간다. 그 선을 넘지 말 것.

---

## 3. 사용자

| | |
|---|---|
| 사용자 | **본인 + 동행 전원.** 같이 가므로 상하 구분 없이 같은 화면을 본다 |
| 편집자 | **본인** — 출발 전 데이터를 채운다. 동행은 코드·문서를 안 봄 |
| 인원 | **남자 4명** · 트윈 2실 |
| 기기 | **모바일 우선** (375×812 기준). 데스크탑은 데이터 편집용 |
| 네트워크 | 여행 중 불안정 전제. 지도 타일 외에는 오프라인 동작 |

---

## 4. 기능 요구사항

### F1. 구역 브라우징 (Must)
- 오사카 시내를 구역으로 나눠 제시 (현재 5개). 한 구역은 **그 안에서 걸어 다닐 수 있는 범위** — 반경 2km 기준 (R-D8)
- 각 구역: 이름 · 접근 역 · 지도 · 카테고리별 장소 목록
- 상단 구역 칩으로 바로가기, 스크롤 시 현재 구역 자동 하이라이트

### F2. 장소 카드 (Must)
- 한글명 · 영문명 · 한 줄 특징 · ★추천 · 🇰🇷한국인 필수
- **탭 → 구글맵 새 탭.** 이 앱의 존재 이유. 다른 어떤 기능보다 우선

### F3. 구역 지도 (Must)
- Leaflet + CARTO 타일. 구역 내 장소를 번호 핀으로
- 핀 ↔ 하단 이름 칩 양방향 하이라이트
- "이 구역 구글맵에서 보기" 버튼
- **동선 표시** (v0.5) — 번호 핀 1→2→3을 점선으로 연결. **데이터 배열 순서가 곧 동선**
  - ⚠ **직선이다.** 도로·골목을 따라가지 않는다. CSP에 `connect-src`가 없어 OSRM 같은 경로 API 호출이 **구조적으로 불가**하고, 완화하지 않는다 (§ 5). 정확한 길찾기는 구글맵 링크가 담당
  - 화면에 "직선이라 실제 길은 아니다"를 명시한다 — 안 쓰면 도로 경로로 오해한다
  - **하루에 한쪽만 도는 원정은 `noRoute`로 끈다** (교토: 남쪽 vs 북서쪽)
  - `base_hotel`에 좌표가 있으면 `숙소 → 장소들 → 숙소` loop + 숙소 핀. **없으면 장소들만 잇는다** (숙소 미정 상태에서도 동작)

### ~~F4. 분류 필터~~ — **폐기** (v0.20)

**§ 7 범위 밖으로 옮겼다.** 카테고리는 카드 소제목(`관광지`/`맛집`/`카페`)으로만 남고, ★·🇰🇷은 뱃지로만 남는다.

폐기 근거는 § 7 표에.

### F5. 원정 섹션 (Could) — 현재 **미사용**
- 하루를 통째로 쓰는 시외 일정용. **이번 여행은 28.5시간이라 교토·USJ·나라·고베를 전부 제외**했다 (`CONCEPT.md § 1`)
- `EXCURSIONS`는 비었지만 **엔진은 그대로 살아 있다** — 객체 하나 push 하면 즉시 되살아난다
- 구성: 가는 법(경로 카드 + 길찾기) · 도착 후 · 할 것 · 원정지 장소

### ~~F6. 접속 게이트~~ — **폐기** (v0.16)
- public 저장소에선 클라이언트 게이트가 원리상 무력하다. 마찰만 남아 제거했다
- **다시 비공개가 필요하면 게이트를 되살리지 말고 private repo 로** (`CLAUDE.md § 6`)

### F7. 맨 위로 (Could)
- 600px 이상 스크롤 시 플로팅 버튼

---

## 5. 비기능 요구사항

| 항목 | 요구 |
|---|---|
| 배포 | **단일 HTML 파일.** 빌드·번들러·npm 설치 없이 더블클릭으로 열림 |
| 외부 의존성 | Leaflet(unpkg) + CARTO 타일 **2개까지만.** 추가 금지 |
| 폰트 | 시스템 폰트만. 웹폰트 로드 금지 (모바일 데이터·로딩) |
| 오프라인 | 지도 타일을 제외한 전 기능 동작 |
| 보안 | CSP `default-src 'none'`. DOM 주입은 `textContent`. `innerHTML`은 정적 SVG 상수만 |
| 저장 | **localStorage 0키.** 사용자 데이터 저장 없음 (v0.16 게이트 제거로 완전 무상태) |
| 성능 | 지도는 IntersectionObserver 지연 init |
| 접근성 | 칩·탭 `aria-pressed`, 접이 항목은 네이티브 `<details>`, `prefers-reduced-motion` 대응 |

---

## 6. 데이터 요구사항

| 항목 | 목표 | 현재 (v0.20) |
|---|---|---|
| 시내 구역 | 반경 2km 이내로 분할 | 5 ✅ (오사카성·베이 9km → 분리) |
| 시내 관광지 | 구역당 4~8 | **25** ✅ 좌표·링크 전수 검증 |
| 시내 맛집 | 구역당 4~8 | **8** ✅ 5끼 배정 완료 · 좌표·휴무·쿼리 전부 검증 |
| 시내 카페 | 구역당 2~4 | **4** ✅ 노포 킷사텐 3 + 아케이드 체인 1 |
| **테마 `서브컬쳐`** | **6~10** | **9** ✅ 덴덴타운·오타로드·e스타디움·e-ZONe·타이토·조신·스루가야·애니메이트·DiCE |
| **테마 `역사·문화`** | **6~10 (서브컬쳐와 동등)** | **6** ✅ 오사카성·천수각·석벽관·호코쿠신사·역사박물관·시텐노지 |
| 원정 | — | **제외 확정** (교토·USJ·나라·고베). EXCURSIONS 비어 있음 |
| 구역 접근 정보 | 필수 | 역 기준으로 작성 ✅ (숙소 비의존) |
| 숙소 정보 | 선택 | ✅ **도미인 프리미엄 난바** 34.6709, 135.5064 — 지도에 숙소 핀 + loop 동선 |

**데이터 품질 기준**
- 좌표: 지도에 찍어 눈으로 확인 (land-check). 미검증 좌표 커밋 금지
- `place_id`: 확실하지 않으면 넣지 않고 쿼리 URL만 사용
- `feature`: 한 줄. "왜 가는지" 또는 "언제 가야 하는지"를 담을 것. 백과사전식 설명 금지
- **정기휴무는 `feature`에 명기, 영업시간은 쓰지 않음** (R-D10). 시간은 변동이 잦아 틀린 정보가 되지만, 요일 휴무는 안정적이고 헛걸음의 주원인이다
- 소요시간·요금: 변동됨. 출발 전 1회 재확인

---

## 7. 범위 밖 (Non-goals)

의도적으로 **안 하는 것**. 요청이 와도 먼저 이 목록을 확인할 것.

| 안 함 | 이유 |
|---|---|
| 사용자 입력·기록·사진·별점 | 읽기 전용 가이드다. 저널이 필요하면 별도 앱 |
| **분 단위** 일정표 | § 2의 설계 전제와 정면 충돌. **단 v0.6의 `zone.schedule` 은 예외** — 시설 마감·배 시각 같은 "어길 수 없는 것"만 시각으로 박고 나머지는 순서만 준다 (§ 2 참조) |
| 실시간 정보 (영업시간·혼잡도·환율) | CSP상 외부 API 호출 불가. 정확성 보증 못 함. **단 정기휴무는 예외 — `feature`에 쓴다** (R-D10) |
| 다국어 | 한국인 일행 전용 |
| 계정·서버·DB | 단일 파일 원칙 |
| 게이미피케이션 (도장·미션·진행률) | 오키나와판(Vol.I)의 자산. 공용 가이드에는 부적합 |
| 예약·결제 연동 | 범위 밖 |
| **분류 필터** (구 F4) | **v0.20에서 폐기.** 지리 구역 시대의 유물이다 — 구역당 장소가 많고 시간표가 없던 시절엔 "이 구역에서 밥집만"이 필요했다. **DAY 기반 + 시간표**로 바뀌자 ① 시간표가 이미 언제 뭘 할지 답을 주고 ② 화면 대부분이 시간표·지도·이동칩이라 카드만 숨겨도 안 줄고 ③ DAY 1은 카드가 2장뿐이라 필터를 쓸 규모가 아니다. **실측: 주력 필터(관광지·추천만)가 페이지를 15%밖에 안 줄였다** |
| **테마 필터** (`서브컬쳐`/`역사·문화`) | **만들지 않는다.** **DAY 칩이 이미 테마를 가른다** (DAY 1 히데요시 / DAY 2 덴덴타운). 만들면 DAY 칩과 정확히 중복. 데이터 비중(9 대 6)은 § 6에서 지킨다 |

---

## 8. 성공 기준

1. 일행 누구나 **설명 없이** 열어서 그날 갈 곳을 스스로 고른다
2. 여행 중 "여기 어디야?" 질문이 사라진다 (탭 → 구글맵)
3. 링크를 다시 찾느라 카톡을 스크롤하는 일이 없다
4. 잘못된 좌표로 엉뚱한 곳에 도착한 사고 **0건**

---

## 9. 버전 히스토리

| 버전 | 날짜 | 내용 |
|---|---|---|
| **v0.1** | 2026-08-09 | 도쿄 친구 가이드에서 이식. 엔진 100% 재사용 + 도쿄판 결함 3건 수정(R-D1 사이드카 JSON 이중 소스 / R-D2 주석 평문 비번 / R-D5 fitBounds 핀 잘림). `DISNEY` 단일 객체 → `EXCURSIONS[]` 배열 일반화, `buildCatSection` 추출로 구역·원정 렌더 통합, 죽은 코드 `buildDineCard` 제거. 데이터는 관광지 시드 24곳(미검증)만 |
| **v0.2** | 2026-08-09 | 좌표 24곳 Nominatim/OSM 대조 완료 — 4곳 정정(난바 야사카 Δ0.39km / 오사카 코리아타운 Δ0.85km / 후시미이나리 Δ0.64km / 기온 하나미코지 Δ0.49km), 영문명으로 조회 실패한 3곳은 일본어 쿼리로 교체. **구역 4 분리** — 오사카성·베이가 9km 떨어져 앱 전제와 충돌(R-D8) → `오사카성·모리노미야` + `베이` 2개로. 그 결과 생긴 1곳짜리 구역에서 `fitBounds` 최대줌 튐 발견 → `setView` 분기(R-D9). `zones[].access`를 숙소 비의존(역 기준)으로 작성 |
| **v0.3** | 2026-08-09 | **개명: 오사카 친구 가이드 → 오사카 여행 가이드.** 본인도 같이 가므로 "친구에게 주는 가이드" 프레임을 폐기하고 "같이 보는" 톤으로 전환 (킥커·게이트·CONCEPT § 4.1) |
| **v0.4** | 2026-08-09 | **문서 체계 재편 + 테마 축 신설.** (1) `DOC-MAP.md`(정본 규칙·영향 매트릭스·고아 자동점검) / `design.md`(디자인 정본) / `learning.md`(회고) 신설. 토큰 값이 `CLAUDE § 5`·`CONCEPT § 5` 두 곳에 중복돼 있던 것을 `design.md` 단일 정본으로 통합하고 **R-D11**(삼각 sync) 신설. 고아 3건 해소(README 인바운드 0, CONCEPT·TRANSPLANT 아웃바운드 0). (2) **`theme` 축 신설** — 동행 요청(서브컬쳐·게임·e스포츠·PC방 + 역사·문화 동등 비중)을 4번째 카테고리가 아니라 **카테고리와 직교하는 축**으로 수용 (F4 · `learning.md § 11`). (3) 권한 경계 완화 — 부모 워크스페이스 **읽기** 허용 |

| **v0.5** | 2026-08-09 | **구역 동선 표시.** 번호 핀 1→2→3을 점선으로 연결 — 재료는 사용자 픽이 아니라 `places` **배열 순서**(읽기 전용 유지). 도쿄 메인의 OSRM 도로 곡선은 CSP에 `connect-src`가 없어 이식 불가로 판단하고 직선 + 명시 문구로 처리. 교토는 `noRoute`로 동선 끔(남쪽/북서쪽 충돌). `base_hotel.lat/lng` 대응 — 좌표가 들어오면 숙소 핀 + loop 동선 자동 생성, 없으면 장소들만 연결. 원정을 zone처럼 재조립해 넘기며 `noRoute`가 유실되던 버그 수정 (`learning.md § 12`) |

| **v0.6** | 2026-08-10 | **DAY 기반 계획표.** 여행 전제가 전면 변경(3박4일 3인 항공 → **2박3일 남자 4명 페리**, 교토 제외)되어 `GUIDE_DATA`를 지리 구역 → **DAY 구역**으로 재편. `zone.tag`(`DAY 1`)·`zone.schedule`(이 날의 흐름) 신설, `buildSchedule` 렌더러 추가. § 2에 전제의 한계를 명시하고 시간표의 선을 규정(시설 마감·배 시각만 못 박음). `EXCURSIONS` 비움(엔진은 유지). 신규 장소 land-check 후 투입 — 도요토미 석벽관·호코쿠 신사·오사카 역사박물관·덴덴타운·오타로드·도구야스지·난바파크스·신사이바시 파르코. `overview.html`을 **자동 생성물**로 전환(`tools/build-overview.js`) — 손 유지에서 오던 이중 사본 위험 제거. 문서 속 bash 추출기가 포맷 앵커라 데이터 재작성 시 죽던 문제 수정(`learning.md § 13`) |

| **v0.7** | 2026-08-10 | **선착장 ↔ 숙소 이동 블록.** `GUIDE_DATA.transfer` 신설 — 도착(ARRIVAL)은 페이지 맨 위, 복귀(DEPARTURE)는 맨 아래에 배치해 페이지가 여행의 시간축이 되도록. 교통편(팬스타 무료 셔틀 → 주오선 → 혼마치 환승 → 미도스지선)·챙길 것(여권·Visit Japan Web·엔화·IC카드)·수속 시각을 명시. **복귀일 위험 발견** — 선사 공지 기준 수요일 코스모스퀘어 셔틀 막차가 13:50이라 기존 "13:30 난바 출발"로는 놓칠 수 있음 → 12:50 출발로 당기고 `warn` 블록으로 경고 |

| **v0.8** | 2026-08-10 | **타입 스케일 · 모바일 범용성.** 모든 `font-size` 를 px → **rem** 으로 전환(87개)하고 `html` 루트에 `clamp(17px, 16px + 0.35vw, 20.5px)` 를 걸어 화면 폭에 따라 글자가 같이 커지도록. **PC에서 글자가 작아 보이던 문제 해결** (리드문 15px 고정 → 16.2px @375 / 19.2px @1280). 배지·라벨 최소 크기 상향 12개(10px→11.5px 등) — 야외·햇빛 가독성. 탭 타깃 44px, `≤359px`/`≥700px`/`≥1000px` 분기 추가, 긴 줄 `62ch` 제한. `overview.html` 생성기도 동일 스케일 적용 |

| **v0.9** | 2026-08-10 | **비번 입력 한글 IME 자동 보정.** 웹에서 IME를 끌 방법이 없어(`ime-mode` 폐기) **끄는 대신 되돌린다** — 두벌식 ↔ QWERTY 역변환. 완성형 음절 분해 + 호환 낱자 51자 매핑, 조합 중(`isComposing`)에는 미개입. 검증 11/11 |
| **v0.10** | 2026-08-10 | **여행 메모 + 일본어 탭 · 구역 날짜.** (1) 도쿄 메인의 `JP_SCENARIOS` 이식 — 10 시나리오 · 71 문장 · 40 팁. **도쿄 고유는 대사관 번호 1건뿐**이라 오사카 총영사관으로 교체 (`learning.md § 14`). (2) `TRIP_NOTES` 신설 — 응급·지진·8월 더위·매너·결제·통신 6블록, 오사카 맥락으로 새로 작성. (3) 항목은 네이티브 `<details>` — 보고 싶은 것만 펴서 본다. (4) `zone.date` 신설 — 헤더 배지 + 내비 칩 앞머리(`8/24 히데요시의 오사카`) |

| **v0.11** | 2026-08-10 | **최상위 탭 3개로 재편.** 하나의 긴 스크롤이던 페이지를 **여행 / 알아둘 것 / 일본어** 로 분리. 성격이 다르기 때문 — "어디를 가나 / 어떻게 대처하나 / 뭐라고 말하나". 구역 칩·분류 필터는 여행 탭에서만 의미가 있어 다른 탭에서는 내비를 통째로 숨긴다. 탭과 내비를 **단일 `.stickybar`** 로 묶음 (각각 `sticky top:0` 이면 겹침) |

| **v0.12** | 2026-08-10 | **장소 심층 정보 19곳.** `place.deep` 신설 — `what`(어떤 곳) / `see`(이건 보고 오자) / `insight`(알고 보면) / `link`. 카드 안 `<details>` 로 접어둬 **보고 싶은 사람만 편다.** 링크 19개 전부 실재 확인(ko 위키 13 · ja 위키 5 · 공식 1, 전부 HTTP 200) — 신설 시설(도요토미 석벽관)은 위키가 없어 오사카성 공식 페이지. 카드 구조 변경: `<a>` 안에 `<details>` 를 못 넣어 바깥을 `div.place`, 지도 링크를 안쪽 `a.p-link` 로 분리 (필터는 `.place` 기준이라 바깥이 유지돼야 함) |

| **v0.13** | 2026-08-10 | **링크 전수 감사 · R-D12 신설.** v0.12의 "19/19 정상"은 `HEAD` 200만 본 것이라 **오배송 2건을 놓쳤다** — 호코쿠 신사(동음이의 페이지), 신사이바시 파르코(회사 문서로 리다이렉트). 링크 종류별 실질 검증으로 교체: **구글맵 19곳을 Nominatim 역지오코딩**해 우리 좌표와 대조(전부 0.22km 이내), 위키는 `redirects`+`pageprops`로 동음이의·최종 제목 확인, 공식 페이지는 렌더 후 내용 확인. 최종 38개 링크(구글맵 19 + 심층 19) 전부 통과 |

| **v0.16** | 2026-08-10 | **비번 게이트 제거 · `overview.html` 폐기 · 이동 경로.** public 저장소에선 클라이언트 게이트가 무력해 마찰만 남아 통째로 제거(스크립트 3→1블록, localStorage 0키). 게이트가 없어지자 `index.html`이 상위집합이 되어 `overview.html`과 생성기 폐기 — 일행 링크를 가이드 1 + 숙소 1로 정리. `METRO` 상수 + `move` 스키마 신설: 현지 안내판과 같은 노선 기호·색(Ⓜ 미도스지 빨강 / Ⓒ 주오 초록)을 보여주고 실제 경로는 구글맵 대중교통 길찾기로 넘긴다(7구간). R-D12로 신규 검색어 검증 중 호텔명 띄어쓰기 문제 발견해 5곳 정정 |
| **v0.17** | 2026-08-10 | **맛집 8곳 투입 — 5끼 배정 완료.** 호텔 조식이 아침 2회를 덮어 채울 끼니는 5끼뿐이었다. 검증에서 **4건을 걸렀다** — 야에카츠(좌표가 중국 쿤밍), 규카츠 카츠규(USJ 근처), 지유켄(월요일 휴무 → 8/24 불가), 도톤보리 이마이(수요일+제4화요일 → 8/25·26 둘 다 불가). `maps_url` 쿼리에 `店`을 붙이면 지오코딩이 실패하는 것도 발견해 4곳 정정. 도톤보리 맛집은 DAY 1 구역에서 `offzone`으로 지도 프레이밍에서 제외 — 오사카성 지도가 안 넓어진다 |

| **v0.18** | 2026-08-10 | **카페 4곳 — 8월 한낮 실내 피난처.** 노포 킷사텐 위주(마루후쿠 1934 · 준킷사 아메리칸 1946 · 아라비야 1951) + 아케이드 체인 1. 검증에서 **또 3건을 걸렀다** — 마루후쿠 검색이 **도쿄 지점**을, 호쿠쿄쿠세이가 **중국 허베이의 직업학교**를 반환했고, 우에시마·타리즈는 우메다·이타미 쪽이었다. OSM 미등록 노포 2곳은 좌표를 생략하고 **공식·주소로 실재만 확인**. 아라비야는 **수요일 부정기 휴무**라 8/26 위험을 에 명기 |

| **v0.19** | 2026-08-10 | **서브컬쳐 6곳** — e-ZONe · 타이토 스테이션 · 조신 슈퍼키즈랜드 · 스루가야 · 애니메이트 · DiCE(24h 넷카페). DAY 2 배열을 **실제 도보 순서로 재배열**(북동 → 사카이스지 남하 → 최남단 → 북서). 총 1,760m, 되돌아가는 구간 없음. 좌표 못 찾은 슈퍼포테이토·만다라케는 제외하되 덴덴타운 `deep.see`에 이름은 남김 |
| **v0.35** | 2026-08-20 | **밤에 뭐하지를 여행 탭에서도 발견되게 (option 2).** 사용자 질문 — 밤 섹션이 알아둘 것에 있어 여행 탭에서 안 보인다. 통째로 옮기는 대신(여행 탭은 DAY 타임라인이라 자유 섹션 슬롯이 없음) DAY 1(8/24) 20:00·DAY 2 밤(8/25) 21:00 시간표 노트에 "알아둘 것 → 밤에 뭐하지" 참고 한 줄씩. 상세는 한 곳에 모으고(안 흩어짐) 발견성만 확보. schedule.n 은 rich() 대상이라 강조 렌더됨 |
| **v0.34** | 2026-08-20 | **"밤에 뭐하지" 섹션 신설** (알아둘 것 탭). 대화로만 논의하고 앱엔 없던 밤 문화를 반영 — 게임바 콘티뉴·우라난바 하시고·잔잔요코초·사보아(얼음 없는 하이볼)·호젠지 요코초·클럽 지라프 + "밤은 8/24·8/25 두 번뿐(평일)"·막차/GO 택시 주의. 장소가 아니라 정보라 TRIP_NOTES 로(정식집처럼 카드로 안 넣음 — 밤 장소는 링크보다 오리엔테이션이 중요). 주유패스는 사용자 판단으로 제외(지하철 패스지만 이번 일정엔 본전 애매) |
| **v0.33** | 2026-08-20 | **전반 검토 — 구글맵 링크 전수 검증 + 한글 표현 점검.** 링크: 좌표 있는 37곳 중 36곳 Nominatim 역검증 0.4km 이내 통과. 카니도라쿠만 쿼리에 `本店`이 붙어 Nominatim 실패(구글은 정상) → 검증되는 `かに道楽 道頓堀`로 교체(좌표 34.6688 일치). 핀 없는 4곳(쿠시카츠 다루마·마루후쿠·하리주·아라비야)은 Nominatim이 정확 좌표를 못 잡아 핀 생략 유지·지어내지 않음(R-D3) — 링크는 구글에서 정상. 한글: 가이드 보이스에 존댓말·시혜조·번역투 0, 반말 정보체 일관 |
| **v0.32** | 2026-08-20 | **매스트헤드에 숙소 지도 + 길찾기.** 사용자 요청 — 최상단 도미인 프리미엄 난바 아래 지도, 탭하면 현재 위치→숙소 길찾기. `buildHotelMap()` 신설: 빈 `sections` 를 넘겨 기존 `initLeafletMap` 의 base_hotel 핀 + `pts.length===1→setView` 경로를 재사용(번호 핀·동선 없음). 길찾기 링크 `destination=숙소 좌표(lat,lng)` 로 지오코딩 애매성 제거, origin 은 구글맵이 현재 위치 자동. 매스트헤드라 어느 탭에서도 최상단에서 접근 |
| **v0.31** | 2026-08-20 | **일본어 발음(kr)에 영문 섞인 5곳 정정.** 사용자가 화면에서 발견("C헷쿠인", "W이F이 노 파스와도"). 전부 외래어·약어(チェック=check · Wi-Fi · ATM)라 한글 음역 때 라틴 첫 글자가 남았던 것 — 첵쿠인/첵쿠아우토(×2)·와이파이·에이티에무 로 수정. 75문장 전수 점검(kr 라틴 0 · 일본어문자 0 · 4필드 완비) |
| **v0.30** | 2026-08-20 | **IC카드 4단계 그림을 앱에 SVG로 임베드.** 사용자가 채팅에서 본 4단계 안내를 일행도 보게 앱 도착 블록에 넣음. 실사진은 CSP(img-src data:+카르토)가 막지만 정적 SVG는 innerHTML 허용(icon()과 동일 정책·§6). `SVG.icocaSteps` 상수 + `.ic-fig` 컴포넌트 + buildTransfer 렌더(arrival 전용). 세로 스텝: 셔틀 하차 → 핑크 자판기(분홍 강조) → 현금 ¥2,000 → 카드/주오선 |
| **v0.29** | 2026-08-20 | **IC카드 자판기 위치를 구글맵 링크로.** 사용자가 "판매 장소 사진·역 맵"을 요청 — CSP(img-src data:+카르토만)가 외부 사진 임베드를 막아 실사진은 못 넣는다(오프라인 단일 파일 + 저작권). 대신 앱 철학대로 도착 블록에 코스모스퀘어역 **구글맵 링크**(zone-map-link 재사용) 추가 → 탭하면 실제 위치·사진·스트리트뷰. 도착 스텝에 "개찰구 옆" 명시, IC 발급 how-to(핑크 자판기·¥2,000)와 세트. buildTransfer 에 stationMapQuery 필드 렌더 신설(arrival 전용) |
| **v0.28** | 2026-08-20 | **ICOCA 실물 발급 how-to.** 일행 중 갤럭시(안드로이드)라 지갑앱이 안 되는 사람용으로 실물 카드 구매 절차를 반영 — 코스모스퀘어역(셔틀 내리는 첫 역)의 **핑크색 자판기**에서 ¥2,000(보증금 500 + 사용 1,500)·현금·무기명. 오사카메트로 전 역 발매 확인. 보증금 반납은 JR 창구뿐이라 페리 귀국 동선엔 없어 "그냥 갖고 오기" 권장. 도착 스텝 d(평문)와 돈·결제 섹션 두 곳에 반영 |
| **v0.27** | 2026-08-20 | **정식(앉아 먹는) 맛집 4곳 + 특산품 값 + 예산 섹션 + 스이카 정정.** 일행이 "정식 형태 식사 선호"라 해서, 서서 먹기·회전 위주이던 맛집에 코스로 앉아 먹는 집을 보강 — 카니도라쿠(게 코스)·하리주(스키야키 노포)·홋쿄쿠세이(오므라이스 발상)·오본식당(다카시마야 8F), 전부 DAY 3. 기념품에 오사카 특산품 값(미즈나스·이와오코시·다시마 츠쿠다니), 알아둘 것에 "예산(현지)" 섹션 신설(페리·숙박 제외 — 현지 1인 알뜰 ¥26,000 / 넉넉 ¥70,000). 도착 블록에 "스이카는 도쿄 카드, 간사이는 ICOCA" 정정. 좌표는 Nominatim land-check(카니도라쿠·홋쿄쿠세이·다카시마야 검증 / 하리주는 미검증이라 핀 생략, 카드·구글맵 링크는 정상) |
| **v0.26** | 2026-08-18 | **귀국편 면세 수령 흐름을 기념품 섹션에 항목으로 추가.** 팬스타 인터넷 면세점 온라인 주문(왕복 기준) → 승선 2일 전 마감 → 수령을 귀국편으로 = 오사카 3일 빈손, 돌아오는 배에서 여권 제시 수령. 검증 못 한 부분(담배 온라인 제외 여부·정확한 주문 화면)은 "팬스타에서 확인"으로 남김 — 셔틀 시각과 같이 |
| **v0.25** | 2026-08-18 | **면세점 위치 명확화 — "터미널 아니라 배 안".** 사용자가 "오사카항에 면세점 있지 않냐" 물었고 흔한 오해라 못박음. 오사카항 국제페리터미널은 매점·면세점·코인로커가 전부 없다(2 소스). 면세점은 선사가 배 위에서 운영 = 선내에만 있다. 공항과 반대 구조 |
| **v0.24** | 2026-08-18 | **overview.html 부활.** v0.16 에 폐기했던 파일인데 일행이 초반에 공유받은 옛 링크가 404 로 죽어 있었다. 페이지 복원이 아니라 index.html 로 튕기는 **리다이렉트 스텁**으로 되살림(쿼리·해시 보존). 이중관리를 피하려 스텁만 남기고, CLAUDE §2 파일표에 "지우지 말 것" 등록 |
| **v0.23** | 2026-08-18 | **기념품 섹션 신설** — 알아둘 것 탭에 🛍. 장소가 아니라 정보라 관광지/맛집/카페 3분류(§3.2)에 안 넣고 TRIP_NOTES 로. 배 여행 기준(선내 면세·팬스타 왕복 귀국편 수령·액체 제한 없음). 오사카 지주 呉春·秋鹿, 약·드럭스토어, 오코노미야키 가루. 551 부타망은 이번에도 제외 근거 명기. 서브컬쳐·주방칼은 이미 동선에 있다고 연결 |
| **v0.22** | 2026-08-18 | **출발 전 리스크 3건.** ① 태풍(8월 말 성수기·양방향 배라 백업 없음) → departure warn + 백로그 🔴 ② 콘센트 정정 — 한국 둥근 2핀은 일본 납작 슬롯에 물리적으로 안 꽂힌다, "돼지코 사실상 필수" ③ 야키니쿠 M 개실 예약을 호텔 프런트 대행으로(도착·짐 맡길 때). 타투 리스크는 사용자가 앱에서 제외 |
| **v0.21** | 2026-08-13 | **미확인 3건 종결.** 배편이 **양방향 17:00 출항**으로 확정(사용자). 남아 있던 🟡 두 건을 공식 페이지로 확인해 데이터에 반영 — 석벽관 `입관 17:30 · 연말연시만 휴관`, e스타디움 `11~21시 · 무휴`, 덴덴타운 5개 점포 **전부 정기휴무 없음**. **덴덴타운 "11:00~19:00" 표기가 틀렸음을 발견** — 그건 개인 상점 기준이고 대형점은 20~24시까지다. 저녁 뒤 2차전이 가능해졌다. 확인하러 화면을 열었다가 **`feature` 가 `rich()` 를 안 타서 카드 37장 중 24장에 `**` 가 그대로 찍혀 있던 것도 발견해 고쳤다** (`learning.md § 18`) |
| **v0.20** | 2026-08-10 | **분류 필터 폐기 + 문서 stale 일괄 정리.** 사용자 지적("눌러도 변화가 없는데 왜 있냐")을 실측으로 확인 — **주력 필터가 페이지를 15%밖에 안 줄였다.** 필터는 지리 구역 시대의 유물이고 DAY+시간표 구조에서 역할이 사라졌다. **테마 필터도 만들지 않기로 결정** — DAY 칩이 이미 테마를 가른다(`learning.md § 16`). 내비가 한 줄이 되고 3,835자 감소. 아울러 F4·F6 폐기 반영, 데이터 현황·백로그·`CONCEPT` TODO·README를 실제와 동기화 |

### 이후 계획

| 버전 | 내용 |
|---|---|
| v0.6 | `theme` 축 구현 (스키마 + 토큰 + 내비 칩) |
| v0.7 | 데이터 투입 — 맛집·카페 · 서브컬쳐 · 역사·문화 |
| v0.7 | 원정 소요시간·요금 재확인, 원정 추가 여부 확정 |
| v1.0 | 전체 스모크 테스트 통과 · 일행 공유 시작 |

---

## 10. 참조

- **위(L0)** — 여행 사실·동행·톤 [`CONCEPT.md`](CONCEPT.md)
- **아래(L1)** — 운영 [`CLAUDE.md`](CLAUDE.md) · 디자인 [`design.md`](design.md)
- **코드** — `index.html`
- **회고** — [`learning.md`](learning.md)
- **계보** — [`OSAKA_TRANSPLANT_GUIDE.md`](OSAKA_TRANSPLANT_GUIDE.md)
- **인덱스** — [`DOC-MAP.md`](DOC-MAP.md)
