# Osaka Travel Guide · DESIGN

_디자인 시스템 **정본**_ · **v1.1** (2026-08-09) · Vol. III

> **이 문서가 디자인의 정본.** `CLAUDE.md § 5`는 운영 중 빠른 참조 요약이고, **충돌 시 이 문서가 이긴다.**
> 변경 순서 **R-D11**: `design.md` → `index.html :root` → `CLAUDE.md § 5` → `BUILD_VERSION` (→ [`DOC-MAP.md § 3.2`](DOC-MAP.md))

---

## 0. 시각적 본질

**"출발 전에 접어둔 종이 지도."** 잡지의 격식은 매스트헤드 한 곳에만 두고, 나머지는 손가락으로 빠르게 훑는 목록이다.

1. **인디고 프레임 위의 페이퍼 지면** — 시리즈 공통 축
2. **포인트 색 단 하나** — 매거진마다 이것만 바뀐다. 오사카는 버밀리언
3. **카드 → 구글맵**이 유일한 목적지 — 다른 장식은 이 탭을 방해하지 않는 선까지

읽기 전용이라 **입력 컴포넌트가 하나도 없다** (v0.16 비번 게이트 제거).

---

## 1. Design Tokens

`index.html :root`가 실행체, 아래가 정본. **양쪽 + `CLAUDE.md § 5` 요약을 함께 고친다 (R-D11).**

### 1.1 시리즈 공통 축 — **변경 금지**

매거진끼리 같은 시리즈로 보이게 하는 유일한 근거다.

| 토큰 | 값 | 역할 |
|---|---|---|
| `--frame` / `--indigo` | `#1a1b4b` | 매스트·내비·푸터 프레임 |
| `--indigo-light` | `#4a4d8b` | 프레임 위 보조선·hover |
| `--page` / `--page-tint` / `--page-warm` | `#f5f3ee` / `#ebe7df` / `#efece4` | 지면 · 눌린 면 · 카드 |
| `--white` | `#ffffff` | 최상단 카드 |
| `--ink` / `--ink-soft` / `--ink-faint` | `#2a2d3a` / `#5a5d6a` / `#8a8d99` | 본문 · 보조 · 메타 |
| `--line` / `--line-faint` | `#d9d6cf` / `#e7e4dd` | 구분선 |
| `--shadow` | `rgba(26,27,75,.10)` | **인디고 기반** (중성 회색 아님) |

### 1.2 포인트 색 — 매거진 식별 축

| 매거진 | 토큰 | 값 | 결 |
|---|---|---|---|
| 도쿄 (Vol.II) | `--tokyo-red` | `#d94848` | 로즈레드 |
| **오사카 (Vol.III)** | **`--osaka-red`** | **`#D8452E`** | **버밀리언** |

**새 매거진에서 바꾸는 것은 이 한 줄뿐.** § 1.1을 건드리면 시리즈가 깨진다.

### 1.3 카테고리 3색 — `CAT` 딕셔너리와 쌍

| 토큰 | 값 | 카테고리 |
|---|---|---|
| `--cat-sight` | `#4A55A3` | `관광지` |
| `--cat-meal` | `#D67025` | `맛집` |
| `--cat-cafe` | `#8B6F47` | `카페` |

**정확히 이 셋이다** (R-D4). 네 번째가 필요해 보이면 § 1.5 테마 축을 먼저 검토한다 — 대개 그쪽이 맞다.

### 1.4 뱃지 전용 — **일반 강조 금지**

| 토큰 | 값 | 전용 대상 |
|---|---|---|
| `--gold` / `--gold-dark` / `--gold-glow` | `#d4af37` / `#a08828` / `rgba(212,175,55,.4)` | **★ 추천** (`.p-star`) |
| `--essential` / `--essential-dark` / `--essential-glow` | `#C0455E` / `#97324a` / `rgba(192,69,94,.26)` | **🇰🇷 한국인 필수** (`.p-essential`) |

새면 뱃지의 신호가 죽는다. 도쿄 R13에서 실제로 새서 생긴 룰이다.

### 1.5 테마 축 — **폐기** (v0.20)

> **만들지 않기로 했다.** 카테고리와 직교하는 "왜 가는가" 축으로 설계했지만,
> 구조가 **DAY 기반**으로 바뀌면서 **DAY 칩이 이미 테마를 가른다** — DAY 1이 히데요시, DAY 2가 덴덴타운.
> 테마 칩을 만들면 DAY 칩과 정확히 중복된다. 데이터(서브컬쳐 9 · 역사·문화 6)는 채워져 있고 `feature`/`deep`이 성격을 설명한다.

**두 테마의 비중은 데이터로 지킨다** (현재 9 대 6). 시각 축은 만들지 않는다.

### 1.6 형태 · 타이포

| 토큰 | 값 | 비고 |
|---|---|---|
| `--r` | `12px` | 카드·버튼 공통. 개별 컴포넌트가 따로 정의하지 않음 |
| **`html` 루트 폰트** | **`clamp(17px, 16px + 0.35vw, 20.5px)`** | ⭐ **타입 스케일의 단일 손잡이** (v0.8) |
| `--pad-x` | `clamp(18px, 5vw, 56px)` | **375 → 데스크탑을 이것 하나로 대응** |

**모든 `font-size` 는 `rem` 이다 (v0.8).** px 고정이던 시절엔 PC에서 글자가 작아 보였다 — 화면이 커져도 15px은 그대로였기 때문이다. 루트 `clamp` 하나로 전 화면이 같이 커진다.
**새 컴포넌트에 px 로 `font-size` 를 쓰지 말 것** — 그것 하나만 스케일에서 이탈한다.

| 화면 | 루트 | 리드문 | 장소명 |
|---|---|---|---|
| 320px | 17.1px | 16.0px | 17.6px |
| 375px | 17.3px | 16.2px | 17.9px |
| 1280px | 20.5px | 19.2px | 21.1px |

**하한**: 배지·라벨도 `0.75rem`(≈13px @375) 아래로 내리지 않는다. 야외·햇빛에서 읽는 화면이다.
| `--sans` | 시스템 스택 (Apple SD Gothic Neo / Pretendard / Malgun / Noto Sans KR …) | 본문 |
| `--serif` | Georgia · Times New Roman · Apple SD Gothic Neo | 매스트 타이틀 · 킥커 |

**웹폰트 로드 금지** (`PRD.md § 5`). 한글은 OS 폰트를 쓴다.

### 1.7 구역 액센트

`ZONE_ACCENTS` — **6색**. 현재 구역 5 + 원정 1 = 6으로 정확히 찼다.
**구역이나 원정을 추가하면 색이 순환·중복된다.** 색 추가가 같은 커밋의 일부여야 한다.

---

## 2. Component Inventory

클래스 → 역할의 정본. **개별 CSS 수치는 `index.html`이 정본**, 여기엔 역할과 제약만.

### 2.1 프레임 (인디고 면)

| 클래스 | 역할 | 제약 |
|---|---|---|
| `.masthead` · `.kicker` · `.mast-title` · `.mast-sub` · `.mast-lede` | 표지 블록 | `OSAKA.` = 도시명+마침표, 시리즈 형식 |
| `.mast-meta` · `.pill` | 총계·호텔 칩 | **`base_hotel: null`이면 호텔 칩 생략** |
| `.nav` · `.nav-row` · `.nav-lbl` | sticky 내비 | 가로 스크롤러. **세로 스크롤 금지 (R-D7)** |
| `.zone-chip` · `.chip` | 구역(DAY) 바로가기 | 상태는 **`aria-current`** 로 표시한다. 분류 필터는 v0.20에서 제거 — 내비는 **한 줄**이고, 필터 시절의 `aria-pressed`·`.chip.rec`·`.chip.ess`·`.nav-row.zones` 점선·`.hide`·`.empty` **CSS는 v0.21에서 같이 지웠다** (JS만 지우고 CSS를 남겨 이중선이 그어져 있었다) |
| `.foot` | 푸터 카운트 | |

### 2.2 지면 (페이퍼 면)

| 클래스 | 역할 | 제약 |
|---|---|---|
| `.zone` | DAY 구역 블록 | 헤더 → 시간표 → 지도 → 카테고리 섹션 |
| `.zone-head` · `.zone-name` · `.zone-tag` · `.zone-access` | 구역 헤더 | 배지는 `zone.tag`(`DAY 1`) → 없으면 `AREA 01` 폴백. `access`는 **역 기준** (숙소 비의존) |
| `.zone-sched` · `.zs-head` · `.zs-row` · `.zs-t` · `.zs-w` · `.zs-n` | **이 날의 흐름** 시간표 | 헤더는 구역 액센트 바탕 + 페이퍼 글자. 시각(`.zs-t`)은 `tabular-nums` 로 자릿수 정렬. `zone.schedule` 없으면 통째로 생략 |
| `.catsec` · `.cat-head` · `.cat-ko` · `.cat-badge` · `.cat-cnt` · `.cat-line` | 카테고리 섹션 | `관광지` / `맛집` / `카페` 소제목 + 개수 |
| `.grid` · `.place` | 카드 그리드 | |
| `.p-top` · `.p-name` · `.p-en` · `.p-feat` · `.p-go` | 카드 내부 | `.p-feat` 한 줄에 **정기휴무 포함** (R-D10) · 본문은 `--ink-soft`, **`**강조**` 조각(`.p-feat b`)만 `--ink`** — 바탕이 흐려서 굵기만으로는 강조가 안 산다 (v0.21) |
| `.p-star` · `.p-essential` | ★ / 🇰🇷 뱃지 | § 1.4 전용 토큰 |
| `.stickybar` | 최상위 탭 + 내비를 묶은 **단일 sticky** | ⚠ 둘을 각각 `sticky top:0` 으로 두면 겹친다. 하나로 묶어야 탭 높이가 rem 스케일로 변해도 계산 없이 붙어 있는다 |
| `.toptabs` · `.toptab` | **최상위 탭 3개** (여행 / 알아둘 것 / 일본어) | 밑줄형 탭. 활성은 포인트색 3px 밑줄 + 인디고 글자. 최소 높이 52px |
| `.ti-item` · `.ti-sum` · `.ph` · `.ph-ja` · `.ph-tips` | 탭 안 접이 항목 | 항목은 네이티브 `<details>`. `summary` 마커를 숨기고 `+ / −` 로 대체. 일본어 원문(`.ph-ja`)이 가장 크다 — **점원에게 화면을 보여주는 용도**라 한글보다 우선 |
| `.zone-date` | 구역 헤더 날짜 배지 | 구역 액센트색 글자 + `--page-tint` 알약. 내비 칩에도 앞머리로 붙는다 |
| `.transfer` · `.tr-head` · `.tr-tag` · `.tr-row` · `.tr-need` · `.tr-warn` · `.tr-tip` | **선착장 ↔ 숙소 이동** (맨 위 ARRIVAL / 맨 아래 DEPARTURE) | **인디고 면**이다 — 지면 위에 프레임색 블록을 놓아 "여행의 바깥 경계"임을 시각적으로 구분. `.tr-warn`은 포인트색 반투명 배경 + 테두리 |
| `.sheet` · `.body` · `.block-h` · `.info-list` | 지면 골격 · 소제목 | |

### 2.3 지도

| 클래스 | 역할 | 제약 |
|---|---|---|
| `.zone-map` · `.zone-map-canvas` | Leaflet 컨테이너 | IO 지연 init + `invalidateSize` |
| `.zm-legend` · `.zm-leg-item` · `.zm-leg-num` · `.zm-leg-name` | 하단 이름 칩 | **핀↔칩 양방향 하이라이트 — 이식 자산 중 가장 손이 많이 간 부분. 건드리지 말 것** |
| `.zm-pin` · `.zm-pin-dot` | 번호 핀 | 아이콘이 좌표 위로 솟음 → **fitBounds 픽셀 padding 필수 (R-D5)** |
| `.zm-pin.is-home` · `.zm-pin-home` | 숙소 핀 (사각형) | `--frame` 인디고. **`base_hotel.lat/lng`가 있을 때만** 그려진다. 번호를 갖지 않는다 |
| (Leaflet polyline) | **동선 점선** | 구역 액센트색 · `weight 3` · `opacity .6` · `dashArray '5, 7'` · `interactive: false`. **CSS 클래스 없음** — JS 옵션으로 스타일링. 색은 `ZONE_ACCENTS` 재사용이라 새 토큰이 필요 없다 |
| `.zone-map-link` · `.zml-*` | "이 구역 구글맵에서 보기" | |
| `.map-pop` · `.leaflet-popup-content` | 팝업 | Leaflet 기본 덮어씀 |

### 2.4 원정

| 클래스 | 역할 | 제약 |
|---|---|---|
| `.excursion` · `.ex-scope` · `.ex-intro` | 원정 블록 | `EXCURSIONS` 가 비어 있어 현재 미사용 |
| `.route-card` · `.rc-*` | 경로 카드 | `rc-best` = 추천 경로 |
| `.dir-btn` · `.dir-*` | 길찾기 | 이름에 `택시`/`didi`/`차로` → `driving`, 그 외 `transit` |
| `.todo-list` · `.todo-item` · `.todo-*` | 해야 할 것 | 제목=행동, 설명=이유 (§ 4.3) |
| `.dine-note` | 식사 안내 | |

> **원정 전용 렌더러 금지 (R-D6).** `EXCURSIONS[].sections`는 zone과 같은 스키마라 `buildZoneMap`·`buildCatSection`·`buildCard`가 그대로 걸린다.

### 2.5 이동 · 유틸

| 클래스 | 역할 | 제약 |
|---|---|---|
| `.mv` · `.mv-lines` · `.mv-c` · `.mv-nm` · `.mv-via` · `.mv-go` | **이동 구간** — 노선 기호 + 구글맵 길찾기 | 기호·색은 `METRO` 상수 = 현지 안내판과 동일 체계. `.mv-hero`는 인디고 면(도착·복귀)용 반전 |
| `.p-deep` · `.pd-sum` · `.pd-what` · `.pd-see` · `.pd-insight` · `.pd-link` | 장소 심층 정보 | `place.deep` 없으면 안 그려짐 |
| `.totop` | 맨 위로 | 600px 이상 스크롤 |
| `.ico` | 정적 SVG | `icon()` — **`innerHTML` 허용되는 유일한 경로** |

---

## 3. Layout

단일 스크롤 페이지. 페이지 분할 없음 (저널 계보와 다른 점).

```
.masthead       인디고 · 킥커 → OSAKA. → 리드 → 총계 pill
.nav (sticky)   구역(DAY) 칩 한 줄
#body
  .zone × 5       헤더 → 지도 → .catsec(관광지/맛집/카페) → .grid
  .excursion × 1  scope → intro → 가는 법 → 도착 후 → 할 일 → .catsec
.foot           카운트 · BUILD_VERSION
.totop
```

**반응형은 두 손잡이** — 글자는 `html` 루트 `clamp`, 여백은 `--pad-x`. 기준은 **375×812**지만 **320px(구형 SE) ~ 데스크탑**까지 깨지지 않아야 한다.

| 분기 | 처리 |
|---|---|
| `≤359px` | `--pad-x` 14px, 시간표 시각 칼럼 52px |
| `≤600px` | 지면 여백 제거, 카드 1열 |
| `≥700px` | 카드 최소폭 300px, 시각 칼럼 74px |
| `≥1000px` | 본문 계열 `max-width: 62ch` — 줄이 길면 읽기 힘들다 |

**탭 타깃 최소 44px** (`.chip` / `.zone-chip`). 걸으면서 누르는 화면이다.
접근성: 칩 `aria-pressed`, `prefers-reduced-motion` 대응.

---

## 4. Voice & Tone

원칙 정본은 [`CONCEPT.md § 4`](CONCEPT.md). 여기엔 **화면에 글자를 쓸 때의 실행 규칙**만.

### 4.1 기본 자세

**"같이 가는 사람끼리 미리 정리해 둔 말투."** 도쿄 친구 가이드는 "내가 가봤으니 알려준다"였지만 오사카는 **본인도 같이 간다.**

- 반말 기조, 명령조 아님 (`~하면 돼`, `~는 아침이 나아`)
- 앱이 자기를 지칭하지 않음 (1인칭 없음)
- 시혜적 설명조 금지 (`~하시면 됩니다` / `꼭 가보세요` / `추천드립니다`)
- 격려·게이미피케이션 금지 (`힘내세요!` `달성!`) — 오키나와(Vol.I)의 자산이다

### 4.2 `feature` 한 줄

**"왜 가는지" 또는 "언제 가야 하는지".** 판정 기준 하나 — **읽고 나서 갈지 말지 결정할 수 있는가.**

| ❌ | ✅ |
|---|---|
| "1583년 도요토미 히데요시가 축조한 일본 3대 명성" | "천수각 + 해자 + 공원. 최소 2시간. 니시노마루 정원에서 정면 뷰" |
| "다양한 게임을 즐길 수 있는 오락실" | "레트로 기체 층이 따로 있음. 평일 저녁이 한산" |

**정기휴무는 이 줄 끝에** (`· 수요일 휴무`). **영업시간은 쓰지 않는다** — 변동이 잦아 반드시 틀린 정보가 된다 (**R-D10**).

**테마별 주의** (§ 1.5)
- `서브컬쳐` — 층별 구성·기체 종류·혼잡 시간대가 결정에 쓰인다. **요금표는 쓰지 않는다** (변동)
- `역사·문화` — 연대·인물 나열 금지. **무엇을 보고 얼마나 걸리는지**를 쓴다. 미술관·박물관은 **월요일 휴관이 기본값**

### 4.3 원정 `todo`

**제목은 행동, 설명은 이유.** 왜인지 없으면 안 지킨다.

- ✅ `아침 일찍 출발` / `기요미즈·아라시야마는 9시가 지나면 사람에 묻힘. 8시 전이면 완전히 다른 장소.`
- ❌ `일찍 출발하세요` / `아침에 가는 것이 좋습니다`

### 4.4 안전·매너

동행에게 배포되는 문서다. 촬영 금지 구역(기온 사유 골목 등)·문화 매너는 **톤을 낮추지 말고 명시**한다.

---

## 5. 톤 앵커

| 컴포넌트 | 앵커 | 차용면 |
|---|---|---|
| `.masthead` | 인쇄 잡지 표지 | 킥커 → 대형 타이틀 → 리드 3단 위계 |
| `.zone-head` (`AREA 01`) | 잡지 목차 | 번호 + 이름 + 한 줄 접근 정보 |
| `.place` 카드 | 가이드북 리스팅 | 한글명 크게, 영문명 작게, 특징 한 줄 |
| `.route-card` | 철도 시각표 | 경로·소요·요금을 같은 리듬으로 |
| `.zm-legend` | 지도 범례 | 번호 ↔ 핀 대응 |

새 컴포넌트를 추가하면 행을 더한다. 앵커가 없으면 톤이 미끄러진다.

---

## 6. 변경 룰

| 상황 | 순서 |
|---|---|
| 토큰 추가·변경 | § 1 갱신 → `index.html :root` → `CLAUDE.md § 5` 요약 → `BUILD_VERSION` (**R-D11**) |
| 새 컴포넌트 | § 2 인벤토리 한 줄 + § 5 톤 앵커 지정 |
| 새 필터·칩을 넣고 싶을 때 | ⚠ **먼저 § 1.5를 읽을 것.** DAY 칩이 이미 무엇을 가르고 있는지 확인하고, 중복이면 만들지 않는다 (`learning.md § 16`) |
| **Vol.IV 이전** | § 1.2 **포인트 색 1개 + 도시명 문자열만** 교체하면 90% 끝. § 1.1·1.6·2·3 그대로 재사용. § 4는 `CONCEPT.md` 먼저 |

**금지** — `--frame`/`--page`/`--ink` 축 변경 · `--gold`/`--essential` 뱃지 밖 사용 · 웹폰트 로드 · 외부 의존 추가 · 다른 매거진 색·폰트 직수입([`DOC-MAP.md § 8`](DOC-MAP.md)) · 이 문서와 `:root` 중 **한 곳만** 갱신

---

## 7. 참조

- **위(L0)** — [`CONCEPT.md`](CONCEPT.md) § 4 톤 · § 5 시각 아이덴티티 / [`PRD.md`](PRD.md) § 5 비기능
- **옆(L1)** — [`CLAUDE.md`](CLAUDE.md) § 5 (이 문서의 운영 요약) · § 8 R-D 룰
- **아래** — `index.html` `:root` + `<style>` / [`learning.md`](learning.md)
- **계보** — [`OSAKA_TRANSPLANT_GUIDE.md`](OSAKA_TRANSPLANT_GUIDE.md) § 4
- **인덱스** — [`DOC-MAP.md`](DOC-MAP.md)
