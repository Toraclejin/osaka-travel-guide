# 오사카 가이드 Transplant 명세 (Vol. III)

> **목적**: 도쿄 친구 가이드(`도쿄 여행매거진/guide/`)를 오사카·교토용으로 이식한 기록.
> **작성일**: 2026-08-09
> **대상 독자**: 다음 매거진(Vol.IV)을 만들 사람 + Claude. **도쿄 폴더를 다시 열지 않아도 되게** 하는 것이 이 문서의 목적이다.

---

## 1. 한 줄 요약

**오사카 가이드 = 도쿄 친구 가이드 엔진 그대로 + 데이터 전면 교체 + 도쿄판 결함 3건 수정 + 원정 모듈 일반화.**

---

## 2. 왜 메인 매거진이 아니라 친구 가이드를 골랐나

도쿄 폴더에는 독립된 앱이 두 개 있었다.

| | 메인 매거진 `index.html` | 친구 가이드 `guide/index.html` |
|---|---|---|
| 규모 | 20,901줄 / 802KB | 1,359줄 / 91KB |
| 용도 | 본인 단독 저널 (계획→기록→매거진 발행) | 구역별 관광·맛집 가이드 |
| 저장 | localStorage + IndexedDB | 없음 |
| 데이터 분리 | 상수가 파일 전역에 산재 (11,162~18,102줄) | 한 곳에 응집 |

이번 여행은 **동행 공유용**이므로 친구 가이드 계보를 골랐다. 개인 저널 기능(사진·별점·인사이트·매거진 발행)은 이 앱의 목적이 아니다.

> 만약 다음에 **본인 단독 저널**이 필요해지면 그때는 메인 매거진 쪽을 이식해야 한다. 그 경우 교체 대상은 약 1,700줄(전체의 8%)이고, 상수가 흩어져 있으므로 **한 곳으로 모으는 작업을 같이 하는 것을 권장**한다.

---

## 3. 그대로 재사용 (엔진 ~700줄, 손대지 않음)

| 영역 | 컴포넌트 |
|---|---|
| DOM 안전 | `E()` textContent 헬퍼 · `icon()` 정적 SVG 전용 |
| 매스트헤드 | 킥커 · 타이틀 · 리드 · 메타 pill |
| 내비 | 구역 칩 가로 스크롤러 · 분류 필터 · 추천/필수 토글 · 카운트 |
| 장소 카드 | `buildCard` · ★추천 / 🇰🇷필수 뱃지 · 구글맵 링크 |
| 지도 | Leaflet init · 번호 핀 · **핀↔이름칩 양방향 하이라이트** · IntersectionObserver 지연 init · `invalidateSize` |
| 필터 | `applyFilter` · 빈 상태 |
| 스크롤스파이 | 가로 스크롤러만 이동하는 칩 하이라이트 |
| 맨 위로 | `setupToTop` |
| 비번 게이트 | SHA-256 · 5회/5분 잠금 · FOUC 방지 inline script |
| CSP | `default-src 'none'` 정책 그대로 |
| 디자인 토큰 | frame / page / ink / gold / essential / cat 3색 |

**핀↔이름칩 연동**은 이 앱에서 가장 손이 많이 간 부분이고 그대로 가져올 가치가 있다. 건드리지 말 것.

---

## 4. 교체한 것 — 정체성 (25줄)

| 위치 | 도쿄 | 오사카 |
|---|---|---|
| `<title>` · `mast-sub` | 도쿄 친구 가이드 | 오사카 **여행** 가이드 — 본인도 같이 가므로 "친구" 프레임 폐기 |
| meta description | 우에노 호텔 기준 도쿄… | 오사카·교토 자유여행… |
| 파비콘 문자 | `T` | `O` |
| 마스트 타이틀 | `TOKYO.` | `OSAKA.` |
| 킥커 | A Friend's Guide · 친구를 위한 도쿄 | A Travel Guide · 같이 가는 오사카 |
| 게이트 타이틀 · eyebrow | 도쿄 / 친구 가이드 · "친구 전용" | 오사카 / 여행 가이드 · "우리끼리" |
| 포인트 토큰 | `--tokyo-red: #d94848` | `--osaka-red: #D8452E` |
| localStorage 키 | `tokyo-guide-auth-v2` 외 3 | `osaka-guide-auth-v1` 외 3 |
| `AUTH_HASH` | (도쿄 비번) | (오사카 비번) |
| 길찾기 origin | `… , Tokyo` | `… , Osaka` (숙소 없으면 `Osaka Station`) |

**시리즈 규칙**: 새 매거진은 **포인트 색 1개 + 도시명 문자열**만 바꾼다. `--frame`(인디고) / `--page`(페이퍼) / `--ink`는 공통 축이므로 건드리지 않는다.

---

## 5. 고친 것 — 도쿄판 결함 3건

이식하면서 발견한 실제 결함. `CLAUDE.md` § 8에 룰로 박제했다.

### 5.1 R-D1 · 데이터 소스 이중화 (심각)

- **도쿄판 상태**: `guide/index.html` 안 임베드 `GUIDE_DATA` + `guide/tokyo_guide_data.json` 두 벌
- **문제**: CSP에 `connect-src`가 없어 `fetch` 불가 → **JSON은 런타임에 읽히지 않는다.** 그런데 파일명·README가 정본처럼 안내하고 있었다
- **실측**: 두 파일이 이미 어긋나 있었음 (임베드 쪽에 `place_id` 누락, 장소 수는 69로 동일)
- **오사카 조치**: 사이드카 JSON을 만들지 않음. 임베드가 유일 정본. 자가 점검 명령 추가

### 5.2 R-D2 · 평문 비번이 주석에 노출

- **도쿄판 상태**: `var AUTH_HASH = '8c06…';  // sha256('tokyo2026')`
- **문제**: "평문 비번 박제 X (SHA-256 해시만)"이라는 설계 의도를 주석 한 줄이 무력화. 소스만 보면 비번을 그대로 알 수 있음
- **오사카 조치**: 주석에서 평문 제거. `CLAUDE.md` § 6에 해시 생성 명령만 기재

### 5.3 R-D5 · fitBounds 가장자리 핀 잘림

- **도쿄판 상태**: `map.fitBounds(L.latLngBounds(pts).pad(0.18))`
- **문제**: `pad()`는 위경도 박스를 비율로 넓힐 뿐 픽셀 여백이 아니다. 핀 아이콘이 좌표 위로 솟은 모양이라 가장자리 핀이 잘린다. 도쿄 구역들은 좌표가 촘촘해 표면화되지 않았지만, **교토 원정처럼 넓게 퍼진 좌표군에서 즉시 재현**됐다 (후시미이나리 핀 하단 잘림)
- **오사카 조치**: `fitBounds(bounds.pad(0.12), { padding: [30, 30] })`

---

## 6. 일반화한 것 — `DISNEY` → `EXCURSIONS[]`

도쿄판의 `DISNEY`는 "하루를 통째로 쓰는 시외 일정"을 담는 **단일 객체**였다. 오사카에서는 교토가 그 자리에 들어가고, USJ·나라·고베가 나중에 추가될 수 있으므로 **배열로 일반화**했다.

### 구조 변경

| 도쿄 | 오사카 |
|---|---|
| `const DISNEY = { … }` | `const EXCURSIONS = [ { id, … } ]` |
| `renderDisney()` | `renderExcursions()` + `buildExcursion(ex)` |
| `disneyDirUrl(mode)` | `excursionOrigin()` + `excursionDirUrl(ex, mode)` |
| 내비 칩 하드코딩 1개 | `EXCURSIONS.forEach` 루프 |
| `.disney` / `.disney-scope` / `.disney-intro` | `.excursion` / `.ex-scope` / `.ex-intro` |
| 앵커 `#disney` | `#ex-{id}` |
| `dineInPark[]` / `dineNear[]` (전용 스키마) | `sections[]` (zone과 동일 스키마) |

### 핵심 결정 — 원정 장소를 zone과 같은 스키마로

도쿄판은 원정 식당을 `{name_ko, name_en, feature, q}` 라는 **별도 스키마**로 두고 `buildDineCard`라는 **별도 렌더러**를 썼다. 그 결과:
- 원정 장소는 지도 핀에 안 나옴
- 상단 분류 필터에 안 걸림
- 카드 스타일이 두 갈래로 갈라짐

오사카는 `EXCURSIONS[].sections`를 zone과 **완전히 같은 스키마**로 만들었다. 그래서:
- `buildZoneMap`을 그대로 재사용 → 교토 지도가 공짜로 생김 (`zone.id`를 쓰지 않는 함수라 가능했다)
- `buildCatSection`을 `renderZones`에서 추출해 공용화 → 분류 필터가 교토 장소에도 걸림
- `eachPlace`를 zone + excursion 양쪽 순회로 확장 → 필터 카운트·총계가 정직해짐
- `buildDineCard`는 **죽은 코드가 되어 제거**

`applyFilter`만 비대칭이다: `.zone`은 통째로 숨길 수 있지만 `.excursion`은 **장소 목록만** 숨긴다. 가는 법·해야 할 것은 분류와 무관하기 때문이다.

---

## 7. 데이터 — 전면 교체

| | 도쿄 | 오사카 v0.1 |
|---|---|---|
| 구역 | 4 (우에노·아사쿠사 / 신주쿠 / 하라주쿠·시부야·다이칸야마 / 긴자·롯폰기·아자부다이) | 4 (미나미 / 기타 / 신세카이·덴노지·쓰루하시 / 오사카성·베이) |
| 시내 장소 | 69 | **17 (관광지만, 시드)** |
| 원정 | 디즈니랜드 1 | 교토 1 |
| 원정 장소 | 5 (식당) | 7 (관광지) |
| 숙소 | Hotel Resol Ueno | **null (미정)** |
| `ZONE_ACCENTS` | 4색 | 6색 (원정 확장 대비) |

### v0.2에서 잡은 것 — 이식 자체가 아니라 **이식으로 드러난** 문제

이식 직후 데이터 검증을 돌리자 도쿄판에서는 표면화되지 않던 문제가 두 개 나왔다. 다음 매거진에서도 같은 순서로 나올 것이다.

**(1) 구역 반경 (R-D8)** — `오사카성·베이`를 한 구역으로 묶었는데 두 지점이 **9km** 떨어져 있었다. 도쿄는 구역들이 원래 촘촘해서 이 문제가 없었고, 그래서 "구역 = 반경 얼마"라는 기준 자체가 도쿄판에 없었다. 오사카는 도시 구조가 남북(미나미↔기타)으로 길고 베이가 서쪽으로 멀리 떨어져 있어 바로 터졌다.
→ **새 도시로 갈 때 구역을 나눈 직후 반경부터 재라.** `CLAUDE.md` R-D8에 점검 명령 있음.

**(2) 1곳짜리 구역의 지도 (R-D9)** — (1)을 고쳐 구역을 쪼개니 장소 1곳짜리 구역이 생겼고, 점 1개는 bounds 크기가 0이라 `fitBounds`가 maxZoom(19)까지 튀었다. 도쿄는 모든 구역이 8곳 이상이라 절대 안 나왔던 경로다.
→ R-D8과 R-D9는 **세트**다. 구역을 쪼개는 순간 이 경로가 열린다.

**(3) 좌표 land-check 방법 확립** — Nominatim(OSM) 대조로 24곳 중 4곳이 300m 이상 어긋난 것을 잡았다 (최대 850m — 오사카 코리아타운). 눈으로 지도만 봐서는 이 정도 오차를 못 잡는다. 영문명으로 조회 실패하는 곳(`梅田スカイビル` `天保山大観覧車` `花見小路通`)은 일본어 정식명으로 재조회하고, `maps_url` 쿼리도 일본어로 두는 게 낫다.

### ⚠ 데이터 미완성 상태 (v0.2 기준)

- ~~좌표 미검증~~ → **완료** (위 (3))
- ~~`zones[].access` TODO~~ → **역 기준으로 작성.** 숙소 비의존이라 숙소가 안 정해져도 동작한다. 확정되면 도보 시간만 덧붙이면 됨
- **맛집·카페 0곳.** 검증 없이 식당을 추천할 수 없어 비워뒀다. 남은 최대 공백
- **`base_hotel: null`.** 가짜 자리표시자를 넣는 대신 `null`로 두고 엔진이 우아하게 생략하도록 처리했다 — **이게 옳은 패턴이다.** 자리표시자를 넣으면 화면에 `TODO`가 그대로 노출되고, 지우는 걸 잊으면 동행에게 배포된다
- **원정 소요시간·요금은 근사치.** 출발 전 재확인 필요

---

## 8. 다음 매거진(Vol.IV)을 만들 때

이번 이식으로 확인된 순서:

```
1. 베이스 선택        동행 공유용 → 친구 가이드 계보 / 본인 저널 → 메인 매거진 계보
2. 폴더 + 복사        .claude/worktrees · .DS_Store · 파생 산출물은 제외
3. 정체성 25줄        title · meta · favicon · 마스트 · 게이트 · 포인트 색 · localStorage 키 · AUTH_HASH
4. 데이터 뼈대        구역 4개 + 원정. 숙소 미정이면 base_hotel: null
5. 엔진 검증          JS 파싱 → 서버 띄우고 지도·필터·게이트 확인   ← 데이터 채우기 전에
6. 문서 4종           CLAUDE / PRD / CONCEPT / TRANSPLANT
7. 데이터 투입        좌표 land-check → 맛집·카페 리서치
8. 스모크 테스트      CLAUDE.md § 10
```

**5번을 4번 뒤에 두는 게 중요하다.** 데이터를 다 채우고 나서 엔진이 안 돌면 원인 분리가 어렵다. 뼈대만으로 먼저 화면을 띄워 확인할 것.

### 포트 규칙

| 매거진 | 포트 |
|---|---|
| 도쿄 메인 | 8766 |
| **오사카 가이드** | **8767** |
| Vol.IV | 8768 |

동시에 여러 매거진을 띄울 수 있도록 겹치지 않게 할당한다.

### v0.4 갱신 — 문서 체계도 이식 자산이다

위 8단계의 **6번(문서 4종)** 은 v0.4에서 **문서 8종 + 위계**로 확장됐다. Vol.IV는 이 구조를 통째로 가져가면 된다:

| 층 | 문서 | 비고 |
|---|---|---|
| L0 | `CONCEPT.md` · `PRD.md` | 여행 사실 / 요구사항 |
| L1 | `CLAUDE.md` · **`design.md`** · `*_TRANSPLANT_GUIDE.md` | 운영 / **디자인 정본** / 계보 |
| L3 | `learning.md` | 회고 |
| INDEX | **`DOC-MAP.md`** | 정본 규칙 · 영향 매트릭스 · 고아 자동점검 |

핵심은 **정본 규칙**이다 — 한 사실은 한 문서만 값으로 쓰고 나머지는 링크만 둔다. 오사카는 토큰 값이 두 문서에 중복돼 있었고 (`learning.md § 9`), 도쿄도 같은 문제를 겪고 `design.md`로 해결했다. **Vol.IV는 처음부터 `design.md`를 만들고 시작할 것.**

⚠ 다만 **문서 목록을 그대로 복사하는 것도 과설계다.** 오사카는 앱이 1,354줄이라 `todo.md`·`VERIFICATION.md`를 의도적으로 만들지 않았다. 신설 조건은 [`DOC-MAP.md § 2.1`](DOC-MAP.md).

---

## 9. 참조

- **위(L0)** — [`CONCEPT.md`](CONCEPT.md) · [`PRD.md`](PRD.md)
- **옆(L1)** — [`CLAUDE.md`](CLAUDE.md) (§ 8 R-D 룰이 이 문서의 § 5에서 나왔다) · [`design.md`](design.md)
- **아래** — `index.html` · [`learning.md`](learning.md) (§ 1·2·5·6이 이 문서 § 5·6의 서사판)
- **인덱스** — [`DOC-MAP.md`](DOC-MAP.md) — 특히 **§ 8 계보 대조표** (저널형 vs 가이드형)
