# Learning — 오사카 가이드

_회귀 · 함정 · 결정의 사후 기록_ · **v1.0** (2026-08-09) · Vol. III

> [`CLAUDE.md § 8`](CLAUDE.md)의 R-D 룰은 "하지 마라"만 말한다. **왜 생겼는지의 서사는 여기 있다.**
> 서사가 없으면 다음 세션이 룰을 "과한 제약"으로 보고 지운다.
> **형식**: 사건 하나 = § 하나 · **신호 → 원인 → 조치 → 다음 매거진 룰**.

**전제**: 앱은 `index.html` 1,354줄 단일 파일, 읽기 전용 공용 가이드, 계보는 도쿄 `guide/` 서브앱.
그리고 — **현지에선 못 고친다.** 출발 전에 틀린 것은 여행 내내 틀린 채로 남는다. 아래 모든 룰의 뿌리다.

---

## § 1. 읽히지도 않는 파일이 정본 행세를 했다 · R-D1

**신호** 도쿄 `guide/`에 `tokyo_guide_data.json`. 파일명과 README가 그것을 정본처럼 안내.
**원인** CSP가 `default-src 'none'`이고 `connect-src`가 없다 → `fetch` 불가 → **런타임에 읽히지 않는다.** 실제 데이터는 임베드된 `GUIDE_DATA`였고, 두 벌은 이미 어긋나 있었다(임베드 쪽 `place_id` 누락). 장소 수만 69로 같아서 동기화된 것처럼 보였다.
**조치** 사이드카 미생성. `ls *.json` 점검 추가.

> **보안 정책이 물리적으로 막아둔 경로 위에 파일을 두지 마라.** 죽은 채로 살아있는 척한다. 개수가 같다고 동기화된 게 아니다.

---

## § 2. 주석 한 줄이 설계를 무력화했다 · R-D2

**신호** `var AUTH_HASH = '8c06…';  // sha256('tokyo2026')`
**원인** "평문은 박제 안 하고 해시만"이 설계였는데, **해시 만드는 법을 친절히 적은 주석**이 그걸 정확히 되돌렸다.
**조치** 주석에서 평문 제거, 생성 명령만 `CLAUDE.md § 6`에. `grep` 점검 추가.

> **주석은 코드와 같은 신뢰 경계 안이다.** 코드에서 지운 비밀을 주석에 적으면 아무것도 지운 게 아니다.

---

## § 3. 좌표는 눈으로는 안 틀린 것처럼 보인다 · R-D3

**신호** 이식 직후 24곳이 "지도에 찍어보니 대충 맞는" 상태.
**원인** 눈으로 하는 land-check는 **바다·논밭**만 잡는다. 같은 동네 안 300~800m는 못 잡는데, 실제로 사람을 헤매게 하는 건 딱 그 오차다.
**조치** Nominatim(OSM) 전수 대조, `Δ ≥ 0.3km`면 정정. **24곳 중 4곳**:

| 장소 | Δ |
|---|---|
| 오사카 코리아타운 | **0.85km** |
| 후시미이나리 | 0.64km |
| 기온 하나미코지 | 0.49km |
| 난바 야사카 | 0.39km |

부수 발견 — 영문명으로 조회 실패하는 곳(`梅田スカイビル`·`天保山大観覧車`·`花見小路通`)은 `maps_url` 쿼리도 **일본어로 두는 편이 검색 성공률이 높다.**

> **눈으로 하는 검증은 0.3km 아래를 못 본다.** 좌표는 기계로 대조한다. **모르는 `place_id`는 지어내지 않는다** — 쿼리만 있는 URL이 틀린 `place_id`보다 항상 안전하다.

---

## § 4. 구역 하나가 9km였다 · R-D8 → R-D9

**신호** v0.1의 `오사카성·베이`. 겉보기엔 멀쩡한 이름.
**원인** 오사카성(34.687,135.526) ↔ 가이유칸(34.654,135.429) **약 9km**. "구역 하나를 고르고 **그 안에서 걸어 다닌다**"(`PRD.md § 2`)는 전제와 정면 충돌.
왜 도쿄에선 안 터졌나 — 도쿄는 구역이 원래 촘촘했다. 그래서 **"구역 = 반경 얼마"라는 기준 자체가 없었다.** 오사카는 남북으로 길고 베이가 서쪽으로 멀어 즉시 드러났다.
**조치** 반경 2km / 도보 30분 기준으로 둘로 분리. → **장소 1곳짜리 구역**이 생김.
**곧바로 두 번째 버그** — 점 1개면 bounds 크기가 0이라 `pad()`를 곱해도 0. `fitBounds`가 **maxZoom(19)까지 튀어** 골목 한 칸만 보였다. → `pts.length === 1`이면 `setView(pts[0], 15)` (**R-D9**).

> **R-D8과 R-D9는 세트다.** 구역을 쪼개는 순간 1곳 구역이라는 코드 경로가 열린다. 그리고 — **새 도시에선 구역을 나눈 직후 반경부터 재라.** 이전 도시에서 안 터진 건 그 도시가 촘촘했기 때문이지 룰이 있었기 때문이 아니다.

---

## § 5. `pad()`는 픽셀 여백이 아니다 · R-D5

**신호** 교토 지도에서 후시미이나리 핀 하단이 잘림.
**원인** `bounds.pad(0.18)`은 **위경도 박스를 비율로 넓힐 뿐.** 핀은 좌표 위로 솟은 모양이라 좌표가 화면 안이어도 아이콘은 잘린다. 도쿄는 좌표가 촘촘해 안 보였고, **넓게 퍼진 원정 좌표군에서 즉시 재현**됐다.
**조치** `fitBounds(bounds.pad(0.12), { padding: [30, 30] })`

> **좌표가 보이는 것과 아이콘이 보이는 것은 다르다.**

---

## § 6. 별도 렌더러는 반드시 갈라진다 · R-D6

**신호** 도쿄 원정(디즈니) 식당이 지도 핀에도 필터에도 안 잡히고 카드 모양도 달랐다.
**원인** 원정 식당만 별도 스키마 + `buildDineCard` 별도 렌더러. 처음엔 "원정은 특수하니까"가 합리적으로 보였을 것이다. 그런데 이후 시내 카드에 쌓인 개선(핀 연동·필터·뱃지)이 **한 줄도 따라오지 않았다.**
**조치** `EXCURSIONS[].sections`를 zone과 **완전히 같은 스키마**로. 그러자 `buildZoneMap`이 그대로 걸려 **교토 지도가 공짜로 생겼고**(`zone.id`를 안 쓰는 함수라 가능), `buildCatSection` 공용화로 필터가 걸리고, `eachPlace` 확장으로 카운트가 정직해졌다. **`buildDineCard`는 죽은 코드가 되어 삭제.**
**남은 비대칭 하나** — `applyFilter`에서 `.zone`은 통째로 숨기지만 `.excursion`은 **장소 목록만** 숨긴다. 가는 법·할 일은 분류와 무관하기 때문. 의도된 비대칭이므로 지우면 버그다.

> **특수해 보이는 것을 별도 렌더러로 빼는 순간 개선이 한쪽에만 쌓인다.** 스키마를 같게 만들 수 있는지 먼저 본다. 같게 만들면 기능이 공짜로 따라온다.

---

## § 7. 영업시간은 쓰면 안 되고 정기휴무는 반드시 쓴다 · R-D10

**신호** 부모 제작 가이드가 "장소 추가 시 **영업시간**을 확인하라"고 한다.
**원인** 절반만 맞다. 일본 가게는 코로나 이후 **영업시간 변경이 잦고**, 박아두면 반드시 틀린 정보가 되는데 현지에선 못 고친다. 반면 **요일 정기휴무(定休日)는 잘 안 바뀌면서** 헛걸음의 가장 큰 원인이다. 위험의 방향이 정반대인 두 정보가 "영업 정보"로 묶여 있었다.
**조치** 정기휴무는 `feature` 안에, 영업시간은 안 씀. `hours` 별도 필드도 안 만듦(`PRD.md § 7`과 충돌 + 스키마만 무거워짐). 미술관·박물관은 **월요일 휴관이 기본값**이라 관광지도 같이 훑는다.

> **변동성으로 정보를 가른다.** 자주 바뀌는 정보는 **아예 안 쓰는 것이 쓰는 것보다 정확하다.**

---

## § 8. "친구 가이드"라는 이름이 톤 전체를 틀리게 했다 · v0.3

**신호** 이식 후 문구가 계속 시혜적이었다. `~하시면 됩니다` / `추천드립니다`가 자꾸 섞임.
**원인** 베이스가 도쿄 **친구** 가이드였고, 그 앱의 전제는 "내가 가봤고, **나는 안 가고**, 너에게 알려준다"였다. 오사카는 **본인도 같이 간다.** 이름표가 화자의 위치를 잘못 잡아두니 문장이 계속 그쪽으로 미끄러졌다.
**조치** `친구 가이드` → **`여행 가이드`** 개명. 킥커 `A Friend's Guide · 친구를 위한` → `A Travel Guide · 같이 가는`, 게이트 `친구 전용` → `우리끼리`. `CONCEPT.md § 4.1`에 "가르치는 위치가 아니라 같이 보는 위치" 명문화.

> **톤은 문장 단위로 못 고친다.** 화자의 위치를 먼저 정하라. 이름표가 틀리면 고쳐도 계속 돌아온다.

---

## § 9. 문서가 서로를 안 가리키고 있었다 · v0.4

**신호** 부모 폴더를 워크스페이스에 추가하며 전체를 훑음.
**원인** 링크 그래프를 그리니 셋이 걸렸다.

| 문서 | 문제 |
|---|---|
| `README.md` | **인바운드 0** — 아무도 안 가리킴 (고아) |
| `CONCEPT.md` | **아웃바운드 0** — 나올 길 없는 막다른 문서 |
| `OSAKA_TRANSPLANT_GUIDE.md` | 동일 |

더 나쁜 것 — **디자인 토큰이 `CLAUDE.md § 5`와 `CONCEPT.md § 5` 두 곳에 값으로** 적혀 있었다. 아직 안 어긋났을 뿐이고, 도쿄가 이미 같은 문제를 겪고 `design.md` 정본 + R12로 해결해 뒀다.
**조치** `design.md`(토큰 유일 정본) · `DOC-MAP.md`(정본 규칙·영향 매트릭스·고아 자동 점검) · 이 문서 신설. 전 문서에 참조 섹션 표준화. **R-D11**(삼각 sync) 신설.
**안 만든 것도 기록** — `todo.md`·`VERIFICATION.md`. 오키나와·도쿄엔 있지만 앱이 1,354줄이고 백로그가 10줄 미만이라, 분리하면 문서 수만 늘고 각자가 얇아진다. 신설 조건은 `DOC-MAP.md § 2.1`.

> **같은 값이 두 문서에 적히면 이미 깨진 것이다.** 아직 안 어긋났을 뿐. **고아는 눈으로 못 찾는다** — 링크 그래프는 기계로 돌린다.
> 문서 체계는 앱 크기에 맞춘다. **큰 프로젝트의 문서 목록을 그대로 복사하는 것도 과설계다.**

---

## § 10. 계보를 착각하면 잘못된 가이드를 따른다

**신호** 부모 `README.md`가 "오키나와를 복사하고 보안 코드 5종(`sanitizeImportedState` 등)을 그대로 옮겨라"라고 지시.
**원인** 워크스페이스에 **두 계보**가 있는데 부모 문서는 하나만 안다.

| | 저널 계보 (오키나와·도쿄 메인) | **가이드 계보 (도쿄 `guide/`·오사카)** |
|---|---|---|
| 사용자 입력 | 있음 | **없음** |
| 그래서 필요한 보안 | import sanitizer 4겹 | **그 경로 자체가 없음** |
| 외부 의존 | 웹폰트·CDN·Unsplash | **2개 상한** |
| 규모 | 12,000~20,000줄 | ~1,350줄 |

오사카에서 `sanitizeImportedState`를 찾으면 영원히 못 찾는다. **없는 게 맞다.**
**조치** `DOC-MAP.md § 8`에 계보 대조표 + "따르지 않을 항목" 명시. 부모 문서 수정은 WRITE 경계 밖이라 하지 않음.

> **상위 문서라고 내 프로젝트를 아는 건 아니다.** 안 맞는 항목은 **안 맞는다고 적어두는 것**이 무시하는 것보다 낫다 — 다음 사람이 같은 혼란을 겪는다.

---

## § 11. 카테고리를 늘리기 전에 축이 맞는지 본다 · v0.4

**신호** 동행 요청 — "서브컬쳐·게임·e스포츠·PC방", 그리고 "역사와 문화도 **같은 레벨로**".
**원인 후보** 반사적으로는 `서브컬쳐`를 4번째 카테고리로 넣게 된다. 그런데 **덴덴타운은 관광지인가 서브컬쳐인가**가 즉시 답이 안 나온다. 오사카성도 관광지이면서 역사·문화다. 이중 소속이 생기는 건 **축이 틀렸다는 신호**다.
카테고리는 "무엇을 하는 곳인가"(관광지/맛집/카페)이고, 서브컬쳐·역사문화는 "**왜 가는가**"다. 서로 직교한다.
**조치** 카테고리 3개 유지(R-D4·F4 무사), **`theme` 필드 + 내비 토글 칩**으로 신설. 이미 `recommended`/`essential`이 같은 계열의 직교 플래그라 엔진에 그대로 얹힌다 — 렌더러 신설 없음(R-D6 준수). "같은 레벨로"라는 요청이 정확히 이 구조다.

> **새 분류를 넣기 전에 기존 축과 직교하는지 본다.** 한 항목이 두 값에 동시에 속하면 카테고리가 아니라 **새 축**이다. 축을 늘리면 조합이 늘고, 카테고리를 늘리면 **모순이 는다.**

---

## § 12. 이식은 기능이 아니라 **제약**부터 확인한다 · v0.5

**신호** "도쿄 친구 가이드·내 가이드 참고해서 동선 표시해줘."

**확인 결과 — 참고 대상 자체가 달랐다.** 도쿄 **친구 가이드**(오사카의 직접 계보)에는 동선이 **아예 없다.** `buildZoneMap`/`initLeafletMap`/`fitBounds`뿐으로 오사카와 같은 수준이고, 오사카는 R-D5·R-D9까지 고쳐서 오히려 앞서 있었다. 동선은 **도쿄 메인 매거진**에만 있었다.

**그리고 그 동선은 그대로 못 온다.** 두 제약이 있었다.

| | 도쿄 메인 | 오사카 |
|---|---|---|
| OSRM 도로 곡선 | CSP에 `connect-src ... router.project-osrm.org` | `default-src 'none'`, connect-src 없음 → **fetch 구조적 불가.** 완화는 § 12 금지 |
| 동선의 재료 | `state.picks[d]` — 사용자가 고른 것 | 사용자 입력이 `PRD.md § 7` 범위 밖 |

**조치** 기능을 포기하지 않고 **재료를 바꿨다.** 사용자 픽 대신 **`places` 배열 순서**를 동선으로 삼았다. 마침 `initLeafletMap`이 이미 배열 순서로 번호를 매기고 있어서 **순서 필드를 새로 만들 필요도 없었다** — 번호·범례·동선이 한 소스에서 나온다. OSRM은 포기하고 직선 점선 + "실제 길은 아니다" 문구로 정직하게 처리했다.

**부수 발견 — 재조립 객체는 필드를 흘린다.** 교토에 동선을 끄려고 `noRoute: true`를 넣었는데 안 먹었다. 원인은 `buildZoneMap({ name: ex.title, sections: ex.sections }, acc)` — 원정을 zone처럼 **재조립**해 넘기는 자리라 새 필드가 조용히 사라졌다. § 6에서 "스키마를 같게 만들라"고 해놓고, 정작 **넘길 때 다시 갈라진** 셈이다.

> **이식은 "그쪽에 뭐가 있나"가 아니라 "이쪽 제약이 뭔가"부터 본다.** 제약을 먼저 보면 기능을 버릴지 재료를 바꿀지가 바로 갈린다.
> 그리고 — **스키마를 통일해도 "넘기는 자리"에서 다시 갈라진다.** 객체를 재조립하는 코드는 필드가 늘 때마다 같이 손봐야 하는 부채다.

---

## § 13. 데이터 **포맷**을 바꾸면 문서 속 파서가 조용히 죽는다 · v0.6

**사건** 여행 전제가 통째로 바뀌어(3박4일 3인 항공 → 2박3일 4명 페리) `GUIDE_DATA` 를 DAY 기반으로 재편했다. 스크립트로 다시 쓰면서 **한 줄 JSON** 포맷이 됐다.

**그 순간 `CLAUDE.md § 8·§ 9` 의 자가 점검 명령이 전부 죽었다.** 원인은 데이터가 아니라 추출기였다:

```js
const s=h.indexOf('const GUIDE_DATA = {'), e=h.indexOf('\n]};', s);   // ← 포맷 앵커
```

`'\n]};'` 는 **데이터가 여러 줄로 예쁘게 들여쓰기 돼 있다**는 가정이다. 한 줄로 바꾸니 `e = -1` 이 되고, `slice` 가 엉뚱한 문자열을 잘라 `SyntaxError` 로 떨어졌다. R-D8 반경 점검·R-D10 휴무 점검이 **통째로 무력화**된 것이다.

**무서운 지점은 실패 방식이다.** 조용히 `false` 를 반환한 게 아니라 **에러로 죽었다** — 그건 오히려 다행이었다. 만약 `slice` 가 우연히 파싱 가능한 조각을 잘랐다면 **잘못된 값으로 "OK"** 를 출력했을 것이다. 자가 점검이 거짓 안심을 주는 게 최악이다.

**조치** 괄호 매칭 추출기로 교체했다. 문자열 안의 `{}` 를 건너뛰며 depth 를 세므로 포맷과 무관하다.

**교훈은 R-D1 의 사촌이다.** R-D1 은 "데이터를 두 벌 두지 마라"였고, 이건 **"데이터를 읽는 방법을 데이터의 생김새에 의존시키지 마라"** 다. 둘 다 *지금은 맞는데 나중에 어긋나는* 종류의 결합이다.

> **자가 점검 명령은 코드다.** 코드와 같은 기준으로 취급해야 한다 — 문서에 박혀 있다고 검증을 면제받지 않는다.
> 그리고 **점검이 죽었는지 스스로 알 수 있어야 한다.** 파싱 실패 시 "OK"가 아니라 명확히 실패해야 한다.

---

## § 14. 잘 만든 자산은 **거의 그대로 온다** · v0.10

도쿄 메인 매거진의 `JP_SCENARIOS`(10 시나리오 · 71 문장 · 40 팁)를 오사카로 가져왔다. 예상은 "절반은 도쿄 전용이라 다시 써야겠다"였는데, **실측 도쿄 고유 항목은 1건**이었다 — 한국대사관 도쿄 전화번호.

이유가 분명하다. 그 데이터는 **도쿄가 아니라 일본**을 다루고 있었다. "여기까지 가주세요", "영어 메뉴 있나요?"는 도시와 무관하다. 도시에 묶인 건 전화번호 하나뿐이었다.

**대비되는 사례가 § 12다.** 같은 도쿄에서 동선(`renderDayRoute`)을 가져오려 했을 땐 **통째로 못 왔다** — OSRM은 CSP가 막고, 픽 기반 재료는 읽기 전용 전제와 충돌했다. 그건 데이터가 아니라 **환경에 묶인 기능**이었기 때문이다.

> **이식 가능성은 "얼마나 잘 만들었나"가 아니라 "무엇에 묶여 있나"가 결정한다.**
> 도시·환경·전제에 묶이지 않은 것은 거의 공짜로 온다. 묶인 것은 재료를 바꾸거나 포기해야 한다.
> 그래서 이식 전 질문은 "이거 좋아 보이는데?"가 아니라 **"이게 무엇에 의존하나?"** 여야 한다.

부수 교훈 하나 — **잔재와 비교는 다르다.** "에스컬레이터는 오사카가 오른쪽, 도쿄는 왼쪽"은 잔재가 아니라 정보다. § 9 점검 5가 이걸 잡길래 예외를 넓혔다. **잔재는 지우는 걸 잊은 것, 비교는 일부러 쓴 것** — 자동 점검이 이 둘을 구분 못 하면 사람이 매번 판단해야 한다.

---

## § 15. **200은 검증이 아니다** · v0.13

19곳에 심층 정보를 넣고 링크를 `HEAD` 로 찍어 **"19/19 정상"** 이라고 보고했다. 사용자가 *"현지에 가서 안 들어가지면 4명이 다같이 시간낭비"* 라며 재검수를 요구했고, 다시 보니 **2건이 틀렸다.**

| 링크 | HTTP | 실제로 도착하는 곳 |
|---|---|---|
| 호코쿠 신사 → `ko:도요쿠니 신사` | **200** | **동음이의 페이지** (오사카 것은 한국어 위키에 없음) |
| 신사이바시 파르코 → `ja:心斎橋PARCO` | **200** | **회사 문서 `パルコ`** 로 리다이렉트 |

**둘 다 200이었다.** 200은 "서버가 응답했다"이지 "맞는 문서다"가 아니다. 특히 **구글맵 검색 URL은 아무 쿼리에나 200을 준다** — 그쪽은 HTTP 검사가 아예 무의미하다.

**제대로 된 검증은 링크 종류마다 다르다:**
- **구글맵** → 쿼리를 Nominatim으로 역지오코딩해서 **우리 좌표와의 거리**를 잰다 (19곳 전부 0.22km 이내 확인)
- **위키** → API `redirects=1` + `pageprops` 로 **동음이의 여부와 최종 제목**을 본다
- **공식 페이지** → SPA면 HTML 이 비어 있다. **렌더 후 내용**을 봐야 한다

**내 실수의 구조**가 § 13과 같다. 거기선 파서가 죽었는데 "OK"가 나왔고, 여기선 링크가 틀렸는데 "정상"이 나왔다. 둘 다 **검사가 검사 대상을 실제로 보지 않았다.**

> **"확인했다"고 말하기 전에, 그 확인이 무엇을 배제하는지 물어야 한다.**
> 200은 404를 배제할 뿐 오배송을 배제하지 않는다.
> 그리고 — **틀린 링크의 비용은 나에게 안 온다.** 현지에서 4명이 멈춰 선다. 검증 강도는 실패 비용에 맞춰야 한다.

---

## § 16. 구조를 바꾸면 **이전 구조의 기능이 유물로 남는다** · v0.20

**신호** 사용자: *"분류 버튼을 눌러도 아래에 큰 변화가 안 보이는데 저게 무슨 의미가 있는 버튼이냐."*

**측정부터 했다.** 인상이 아니라 숫자로 확인해야 지울지 말지가 갈린다.

| 필터 | 페이지 높이 |
|---|---|
| 관광지 (주력) | **85%** |
| 추천만 | **86%** |

**주력 필터 두 개가 화면을 15%밖에 안 바꿨다.** 사용자 말이 맞았다.

**왜 그렇게 됐나.** 필터는 이 앱이 **지리 구역 5개**이던 시절의 기능이다. 그땐 "미나미 구역에서 밥집만 보자"가 실제로 필요했다. 그런데 **DAY 기반 + 시간표**로 재편하면서 전제가 무너졌다 — ① 시간표가 이미 언제 뭘 할지 답을 준다 ② 화면 대부분이 시간표·지도·이동칩이라 **카드만 숨겨봐야 페이지가 안 줄어든다** ③ DAY 1은 카드가 2장이라 필터를 쓸 규모가 아니다.

**더 나쁜 건 내가 그걸 못 봤다는 점이다.** 바로 앞 답변에서 나는 *"테마 필터(서브컬쳐/역사·문화)를 추가하자"* 고 제안했다. **DAY 칩이 이미 테마를 가르고 있는데도** — DAY 1이 히데요시, DAY 2가 덴덴타운이다. 만들었으면 DAY 칩과 정확히 중복되는 기능을 하나 더 얹을 뻔했다.

> **기능을 더할 이유는 늘 찾기 쉽고, 뺄 이유는 누가 물어봐야 보인다.**
> 구조를 바꿀 때 **새로 만들 것**만 보고 **낡아버린 것**은 안 본다. 그래서 유물이 쌓인다.
> 재편 커밋에는 "무엇을 더하나"와 함께 **"이제 무엇이 필요 없어졌나"** 를 같이 적어야 한다.

부수 교훈 — **"쓸모없으면 지워야 하는 거 아니냐"는 질문에 방어하지 않는다.** 측정해서 맞으면 지운다. 이번엔 3,835자와 UI 한 줄이 사라졌고 내비가 더 읽기 쉬워졌다.

---

## § 17. **집단의 평균값이 개별 사실인 척 앉아 있었다** · v0.21

**신호** 사용자: *"석벽관, 덴덴타운은 알아서 해줘라."* — 백로그에 🟡로 남겨둔 두 건을 닫으라는 지시.

닫으러 갔다가 **미확인 항목이 아니라 이미 적혀 있던 문장이 틀렸다는 걸** 발견했다.

덴덴타운 카드에는 이렇게 적혀 있었다 — `대체로 11:00~19:00`. 출처가 있는 값이다. **상점가 전체의 영업 시간대**로 공개된 숫자였고, 나는 "확인된 것만 쓴다"는 원칙을 지켰다고 생각했다.

**개별 점포를 확인해 보니 이랬다.**

| | 오픈 | 마감 | 정기휴무 |
|---|---|---|---|
| 조신 슈퍼키즈랜드 | 10:00 | **20:00** | 없음 |
| 애니메이트 | 11:00(평일) | **20:00** | 없음 |
| 스루가야 | 10:00 | **21:00** | 없음 |
| 타이토 스테이션 | 10:00 | **24:00** | 없음 |
| e-ZONe | — | **24시간** | 없음 |

`19:00`은 **개인 상점 기준**이었다. 우리가 실제로 갈 대형점은 하나도 7시에 닫지 않는다.

**그게 계획을 바꿨다.** 백로그에는 *"덴덴타운 상권은 대체로 11:00~19:00. 8/25 저녁을 신세카이로 내리는 이유이기도 하다"* 라고 적혀 있었다. **틀린 근거로 옳은 결론을 냈던 것이다** — 저녁을 신세카이로 옮긴 진짜 이유는 쿠시카츠가 거기 있어서지, 덴덴타운이 닫아서가 아니다. 근거가 틀린 채로 남아 있었으면 **다음에 일정을 흔들 때 잘못된 제약으로 작동했을 것이다.**

> **범위에 대한 사실을 개별 항목에 붙이면 그 순간 오해가 된다.**
> "상점가는 11~19시"는 참이고 "이 가게는 11~19시"는 거짓인데, 카드 한 줄에서는 둘이 구별되지 않는다.
> 집합의 속성을 원소 자리에 쓸 때는 **집합이라고 명시**하거나, 원소를 직접 확인하거나 — 둘 중 하나다.

부수 교훈 — **🟡(확인 필요)로 적어둔 것은 반드시 닫는다.** 이번 건은 사용자가 지시해서 닫혔다. 지시가 없었으면 그대로 배포됐을 것이고, "확인 못 했다"는 꼬리표는 문서에만 있고 **동행이 보는 카드에는 없었다.**

---

## § 18. **문법을 만들었는데 절반에만 적용했다** · v0.21

덴덴타운 데이터를 고치고 브라우저에서 확인하다가, 내가 방금 넣은 문장이 이렇게 보이는 걸 봤다.

```
정기휴무는 **전부 없다**. 다만 **개인 상점은 11:00~19:00**, …
```

별표가 그대로 찍혀 있었다. 세어 보니 **카드 37장 중 24장**이었다.

**원인은 단순하다.** `rich()` 는 `**강조**` 를 `<b>` 로 바꾸는 헬퍼고, 보안 정책(§ 6) 때문에 `innerHTML` 대신 조각을 나눠 `textContent` 로 넣도록 공들여 만든 함수다. 그런데 호출된 곳은 `deep` · `warn` 뿐이었다. **카드에서 제일 많이 읽히는 `feature` 는 `E('p','p-feat', p.feature)` — 순수 `textContent` 였다.**

```js
if (p.feature) a.appendChild(E('p', 'p-feat', p.feature));          // before
if (p.feature) a.appendChild(rich(E('p', 'p-feat'), p.feature));    // after
```

**왜 안 보였나.** `**` 는 **깨진 것처럼 안 생겼다.** 렌더가 실패하면 보통 빈 화면이나 `[object Object]` 가 나오고 그건 눈에 띈다. 그런데 별표는 마크다운을 아는 사람 눈에 *강조하려던 자리* 로 자연스럽게 읽힌다. 그래서 v0.12에 `rich()` 를 만든 뒤 **아홉 개 버전 동안** 살아남았다.

> **문법을 도입하면 그 문법이 등장할 수 있는 필드를 전부 세어야 한다.**
> `rich()` 를 만든 시점의 작업은 "deep 에 강조를 넣는 것"이었고, 나는 그 작업만 끝냈다.
> **헬퍼를 만든다는 건 규칙을 만든다는 뜻이고, 규칙은 적용 범위를 같이 정해야 규칙이다.**

덤으로 하나 더 나왔다 — `.p-feat` 는 `--ink-soft`(흐린 회색)라서 `<b>` 를 붙여도 **굵기만으로는 강조가 안 살았다.** `.p-feat b { color: var(--ink) }` 를 같이 넣었다. 표시 버그를 고치니 **원래 의도했던 강조가 그제서야 보인다.**

부수 교훈 — **"데이터를 고치러 갔다가 렌더러 버그를 찾았다."** 화면을 실제로 열어보지 않고 데이터만 고쳤으면 못 봤다. `node` 검증 6종은 전부 통과하고 있었다.

**그리고 § 16이 바로 다시 걸렸다.** CSS를 훑다가 v0.20 필터 제거의 잔해가 그대로 남아 있는 걸 봤다.

| 남아 있던 것 | 증상 |
|---|---|
| `.nav-row.zones` 점선 밑줄 | 아래 필터 줄과 가르던 선인데, 그 줄이 없어지자 `.nav` 실선 위 1px에 점선이 겹쳐 **이중선** |
| `.chip[aria-pressed]` · `.chip.rec` · `.chip.ess` | 눌리는 칩이 이제 없다 |
| `.place.hide` · `.catsec.hide` · `.zone.hide` · `.empty` | `hide` 를 붙이는 코드가 0곳 |
| 떠 있는 `}` 하나, 빈 `/* 접속 비번 게이트 */` 주석 | v0.16·v0.20 절단면 |

**v0.20에서 "기능을 지웠다"고 쓰고 JS만 지웠던 것이다.** § 16의 교훈을 적어놓고 같은 커밋에서 절반만 실행했다. 지우는 작업은 **JS·CSS·HTML·문서 네 군데**를 봐야 끝난다.

---

## 한 줄들

1. 보안 정책이 막아둔 경로 위의 파일은 죽은 채로 살아있는 척한다 (§ 1)
2. 코드에서 지운 비밀을 주석에 적으면 아무것도 지운 게 아니다 (§ 2)
3. 눈으로 하는 좌표 검증은 0.3km 아래를 못 본다 (§ 3)
4. 새 도시에선 구역을 나눈 직후 반경부터 재라 (§ 4)
5. 좌표가 보이는 것과 아이콘이 보이는 것은 다르다 (§ 5)
6. 별도 렌더러를 만들면 개선이 한쪽에만 쌓인다 (§ 6)
7. 자주 바뀌는 정보는 안 쓰는 게 쓰는 것보다 정확하다 (§ 7)
8. 톤은 문장이 아니라 화자의 위치에서 나온다 (§ 8)
9. 같은 값이 두 문서에 적히면 이미 깨진 것이다 (§ 9)
10. 상위 문서라고 내 프로젝트를 아는 건 아니다 (§ 10)
11. 한 항목이 두 값에 속하면 카테고리가 아니라 새 축이다 (§ 11)
12. 이식은 "그쪽에 뭐가 있나"가 아니라 "이쪽 제약이 뭔가"부터 본다 (§ 12)
13. 스키마를 통일해도 객체를 재조립해 넘기는 자리에서 다시 갈라진다 (§ 12)
14. 데이터를 읽는 방법을 데이터의 생김새에 의존시키지 마라 (§ 13)
15. 자가 점검 명령도 코드다 — 죽었으면 "OK"가 아니라 죽었다고 말해야 한다 (§ 13)
16. 이식 가능성은 품질이 아니라 **무엇에 묶여 있나**가 결정한다 (§ 14)
17. 잔재는 지우는 걸 잊은 것, 비교는 일부러 쓴 것 — 점검이 둘을 구분해야 한다 (§ 14)
18. HTTP 200은 404를 배제할 뿐 **오배송을 배제하지 않는다** (§ 15)
19. 검증 강도는 **실패 비용**에 맞춘다 — 틀린 링크의 대가는 현지에서 치른다 (§ 15)
20. 구조를 바꾸면 이전 구조의 기능이 **유물**로 남는다 — 재편 시 "뭘 뺄까"도 같이 적는다 (§ 16)
21. 기능을 더할 이유는 늘 찾기 쉽고, 뺄 이유는 누가 물어봐야 보인다 (§ 16)
22. 집합의 평균값을 원소 자리에 쓰면, 출처가 있어도 거짓말이 된다 (§ 17)
23. **틀린 근거로 옳은 결론**이 나오면 결론이 맞아서 안 고쳐진다 — 다음 판단에서 터진다 (§ 17)
24. 문서에만 붙인 "확인 필요" 꼬리표는 **동행이 보는 화면에는 없다** (§ 17)
25. 헬퍼를 만드는 건 규칙을 만드는 것이다 — **적용 범위를 같이 정해야 규칙**이 된다 (§ 18)
26. 깨진 것처럼 안 생긴 버그가 제일 오래 산다 — `**` 는 9개 버전을 살아남았다 (§ 18)
27. 데이터 검증이 전부 통과해도 **화면을 안 열면 렌더러 버그는 안 잡힌다** (§ 18)
28. 기능을 지우는 작업은 **JS·CSS·HTML·문서 네 군데**를 다 봐야 끝난다 — v0.20은 JS만 지웠다 (§ 18)

---

## 참조

- **위(L0)** — [`CONCEPT.md`](CONCEPT.md) · [`PRD.md`](PRD.md) § 9
- **옆(L1)** — [`CLAUDE.md`](CLAUDE.md) § 8 (여기의 서사가 룰이 된 곳) · [`design.md`](design.md)
- **계보** — [`OSAKA_TRANSPLANT_GUIDE.md`](OSAKA_TRANSPLANT_GUIDE.md) (§ 1·2·5·6의 원출처)
- **인덱스** — [`DOC-MAP.md`](DOC-MAP.md)

> 새 사건은 **§ 13부터** 이어 쓴다. 반복되면 `CLAUDE.md § 8`의 R-D 룰로 승격시키고 여기엔 서사를 남긴다.
